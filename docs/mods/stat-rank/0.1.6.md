# Stat Rank 0.1.6 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 첫 모드). **기능 변경 없음.**
설치: `Mods\Additional_Functions\Restitutor_Additional_Stat_Rank.dll`, SHA256 `1F418BC954030712ED670CB81B8F15E15135C0CFEF5E323BB4EA000F833A830E`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `StatRank/archive/0.1.5/EntryPoint.cs`, 이전 DLL `StatRank/releases/0.1.5/` (25DFD9D0…).

## 바뀐 것
- Postfix 2개(`UICharacterView.RefreshTipsRoleInfo()`, `UICharacterView.HideHook()`) 등록을 `HookSet.Hook(..., args: Type.EmptyTypes)` 로. 기존 `GetMethod(name, Instance|Public|NonPublic|DeclaredOnly, EmptyTypes)` 와 같은 메서드: 실제 interop 메타데이터에서 `MethodLookup.Unique` 와 같은 조건으로 각 1개 확인(hookcheck).
- GameAssembly 해시 확인·설치 실패 시 전체 해제는 `InstallAll` 안에서 그대로(순서: 해시 → 훅 2개 → enabled).
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성. 처리기·팁 코드는 그대로.

## 검증
Release 빌드 경고/오류 0. 규칙 검사 9개 통과(기존). 등록 대상 메타데이터 확인 2/2. 게임 확인 전.
