# Rebalance Growth 0.3.1 (2026-09-22) — 0.3.0 게임 테스트 반영

게임 확인 전. VM 빌드 경고/오류 0, 규칙 검사 PASS 1536. SHA256 `0525549A…A0C4E4`. Stat Rank 0.2.1 과 함께 배포.

## 사용자 보고 (0.3.0 테스트, 2026-09-22 20:49~21:02 로그) → 원인 → 수정
1. **Q/E 아이콘 안 보임.** 원인 [본문+표]: `IconUtils.GetInputKeyIcon(id)` 은 UIInputKey 표(chunk 218)의 Addressables 경로(`…/keyboard/ui_common_keyboard_q.png` 등)를 돌려줌. 일반 `GLoader` 는 이 경로를 못 읽음 — 게임은 `Core.NewUISystem.MyGLoader`(FairyGUI 로더 확장, LoadExternal 0xD444A0)로 읽음. 수정: `UIObjectFactory.NewObject(ObjectType.Loader)`(= MyGLoader)로 생성.
2. **능력치 미리보기가 다음 열과 겹침.** 로그: 행 폭 365, 숫자 칸 x 245, 미리보기 x 350. 수정: 숫자(texProp)를 능력치 이름 바로 뒤로 당기고(원본 +N 칸 texAddNum 도 같은 만큼), 미리보기를 숫자(또는 원본 +N 이 보이면 그 뒤) 바로 뒤에 붙임. 글자 72%, 형식 `+1 · 60→15%`. 위치 로그 `[level slider] ability row … value x A -> B …`.
3. **스킬 포인트 예정치.** 오른쪽 위 '스킬 포인트:N'(UICom_RoleInfo texTitleSkillPoint/texSkillPoint)를 왼쪽으로 당기고 뒤에 `+N`(선택한 레벨 동안 받을 포인트, LevelGetSkill 간격 = Cheats Skill 반영) 표시. 로그 `[level slider] skill points …`.
4. **스페이스 연타 시 누적 %가 안 쌓임**(D 10→20 고정). 원인 [로그]: `Lv 12+1 … progress 500/…` 가 Lv 47 까지 같은 값에서 시작. 레벨업 중에 다시 누른 입력도 `OnClickBtnLevelUp` Prefix 를 거치는데(원본은 곧바로 반환), 그 Prefix 가 대기 중 누적값을 지워 AniResult 에서 저장되지 않음. 수정: Prefix 에서 지우지 않음. 새 판정이 받아들여질 때만 교체, AniResult 에서 인물 일치 확인. (같은 원인으로 0.3.0 테스트 세이브의 능력치가 몇 번 더 올랐음: 같은 누적값에서 여러 번 계산됨.)
5. **스페이스 = 슬라이더 무시하고 항상 1레벨, 0 이어도 1레벨.** 원인 [본문]: Space/패드 A → `OnAction_A`(0xB80220) → 숨긴 1레벨 버튼 FireClick → 원본 1레벨. 수정(사용자 요청): 숨긴 1레벨 버튼의 클릭 처리를 패널 확인 버튼과 같은 처리로 교체(OnInit 의 EventBridge 콜백 Clear 후 등록) → 슬라이더 값만큼 레벨업, 1레벨 미만이면 경험치 저장, 0 이면 아무 일 없음.
6. **HP/공격 확인(사용자 요청: Tab 스샷 마린 Lv16 HP 153·공격 62 을 공식과 비교).** Hero 121 기본 HP 93·공격 32·신체 15, 로그의 레벨별 신체 증가로 15회 레벨업 계산: /250 → HP 153·공격 62 (**일치**), 원본 /100 이면 HP 168. 검사에 추가.
   - 발견: 레벨업 창의 HP/공격 '+N'(`UIHeroLevelUpView.GetAddPropByType` 0xB86A20)은 원본 /100 식 그대로라 실제보다 크게 표시(스샷 Lv106 신체 79: 표시 +7/+4, 실제 +5/+3). 수정: 같은 입력으로 /250 합계를 돌려주는 Postfix.

## 패치 추가
| 대상 | 종류 | 목적 |
|---|---|---|
| UIHeroLevelUpView.GetAddPropByType(ERolePropertyType) | Postfix | HP(8)/공격(7) 표시 증가량 /250 |
| (UI) BtnUp.onClick | 콜백 교체 | Space/패드 A = 슬라이더 확인 |
