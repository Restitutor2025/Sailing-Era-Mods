# CTRL Instant 0.1.7 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 묶음 2). **기능 변경 없음.**
설치: `Mods\Bug_Fixes\Restitutor_BugFixes_CTRL_Instant.dll`, SHA256 `66D038C7590D8E364B8D89E0C267E5A927D6B992A050267CF943365EF6023D31`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `archive/`, 이전 DLL `releases/0.1.6/` (570684DD…).

## 바뀐 것
- 훅 7개 등록을 `HookSet.Hook` 으로. 기존 `Single(이름)` DeclaredOnly 와 같은 조건, 대상·처리기·순서 동일(hookcheck 7/7).
- 설치 실패 시 오류 기록은 기존 `Error(ex)` 대신 `InstallAll` 의 log.Error 1줄(동작 동일: 전체 해제, enabled=false).
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성.

## 검증
Release 빌드 경고/오류 0. 소유권·수명 검사 13 통과. 게임 확인 전.
