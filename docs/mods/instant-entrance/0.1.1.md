# Instant Entrance 0.1.1 (2026-09-22)

Restitutor.Core 로 전환한 첫 모드(CORE_PLAN 3단계). **기능 변경 없음.**
설치: `Mods\Additional_Functions\Restitutor_Additional_Entrance.dll`, SHA256 `431F5B1AC88835BC479F565B22148B02BB581E31E068C4E56F14E3DE647096E1`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스: `InstantEntrance/archive/0.1.0/EntryPoint.cs`.

## 바뀐 것
- 훅 3개 등록을 `HookSet.Hook` 으로. 대상·인자 형식·처리기·등록 순서 0.1.0 과 동일(검사로 확인). 처리기 본문은 한 글자도 안 바뀜.
- 설치 실패 시 전체 해제는 `HookSet.InstallAll` 로(동작 동일, 오류 문구만 다름).
- Core 버전 확인 추가. Core 파일이 없으면 오류 1줄 후 이 모드만 비활성(게임에서 미확인).
- 로드 로그: `0.1.1 (Restitutor.Core 0.1.0): port-note entry animation and initial 2.5-second harbour gate patched. …`

## 검증
Release 빌드 경고/오류 0. 관리 코드 검사 15개 통과(기존 10 + Core 경유 등록 5). Unity/Harmony 네이티브 동작·화면 결과는 미검증.
