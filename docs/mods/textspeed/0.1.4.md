# Restitutor_fixes_textspeed.dll 0.1.4 — 2026-09-21 (완료)

**상태: 완료.** 2026-09-21 사용자 보고 "완료 잘 적용돼" — 빌드·설치·게임 내 동작 확인. 세부 확인 항목별 결과는 따로 보고되지 않음.

## 요청과 결정 [사용자]
"빠르게" 모드에서 CTRL을 누르지 않았을 때 문장이 다 나온 뒤 **3초** 있다가 다음 대사로. CTRL 빨리 넘기기 중에는 원본 그대로. **오토(자동 대화) 켜짐·꺼짐 모두 적용** (처음엔 오토 켜짐만 → 같은 날 사용자 변경: "빠르게 모드는 텍스트가 바로 다 나오므로 오토 구분 의미 없음").

## 원본 근거 [본문, GameAssembly 50D53D17…]
- `UIDialogView.EndTyping` 0x1349E40: model.IsTyping=false. **model._autoTalk일 때만** model.clickWait=true, 지연 = IsSpeed ? 200/x ms : **500 ms**의 Trigger 등록(콜백 `<EndTyping>b__56_0`).
- `<EndTyping>b__56_0` 0x134F490: clickWait가 true일 때만 `UIDialogCtrl.OnAction_ContinueTalk`.
- `OnAction_ContinueTalk` 0x1343B10: clickWait=false (0x1343CC5).
- `OnAction_FastForwardTalk` 0x1344800(CTRL 누름): 선택지 없을 때 clickWait=0, isAuto=_autoTalk 보관, **autoAndSpeed=true**, 대사 속도 상향, 오토 강제 켬, View.Speed. `_FreeUp` 0x1344A40(뗌): autoAndSpeed=false, _autoTalk 복원, SpeedEnd.
- `StartAutoTalk` 0x1345A90: 오토 토글(OnAction_X, 대화창 OnInit 등록 클릭에서 호출).
- 타이핑 완료 콜백이 EndTyping이라는 연결은 [추론]: 직접 호출자가 없고, StartEffect가 IsTyping=true 후 TypingEffectObject.Start에 대리자를 넘김.
- CTRL = 빨리 넘기기 키 배정은 기존 문서 기록 [문서], 이번에 입력 매핑 미확인.

- `OnAction_ContinueTalk` 0x1343B10 = 플레이어의 "계속" 입력 처리. 자체 가드: ctrl._canClick, 대사 유형 8·9·13·14 제외, model.canClick, 선택지 표시 중 CanClickOption, effect. 오토 꺼짐 상태에서 호출해도 클릭과 같은 경로.
- `EndTyping`은 진입 시 model.IsTyping이 false면 아무것도 하지 않음(0x1349ECC).
- FastForwardTalk: 오토가 꺼져 있었으면 누르는 순간 스스로 OnAction_ContinueTalk 호출(타이핑 중 아닐 때), 켜져 있었으면 속도만 올림.

## 변경 (훅 2개 추가 → 총 7개)
- `UIDialogView.EndTyping` Prefix(진입 시 IsTyping 기록)+Postfix: 실제 문장 완료 + 저장 모드 Instant + `UIDialogCtrl.autoAndSpeed` false일 때 → (오토 켜짐이면) clickWait=false로 원본 500 ms 트리거 무력화, 자체 대기 등록(대화창, TalkTid, unscaledTime+3초). 오토 꺼짐이면 원본은 아무것도 걸지 않았으므로 자체 대기만.
- `UIDialogCtrl.OnAction_ContinueTalk` Prefix(차단 안 함): 원본이 스스로 진행하면(클릭·키·오토 꺼짐 상태의 CTRL 누름) 대기 취소 → 이중 진행 방지.
- LateUpdate: 대사 변경·다시 타이핑이면 취소. CTRL(autoAndSpeed)이 대기 중 눌렸고 대기가 살아 있으면(=오토 켜짐 상태) 즉시, 아니면 3초 후 원본 `OnAction_ContinueTalk()` 호출. HideHook에서 대기 제거.
- 3초는 `Time.unscaledTime`(대화 중 게임 일시정지와 무관). 메뉴를 연 동안에도 흐른다 [설계 선택].
- 로그 `[AutoHold]` 최대 60줄.
- 이전 소스·DLL: `TextSpeed/archive/0.1.3/`.

## 확인 수준
클라우드에 dotnet 없음: 컴파일·StoreTests·실전 모두 미실행. 인터롭 이름은 Assembly-CSharp.dll 문자열로 대조(`_model`, `clickWait`, `_autoTalk`, `IsTyping`, `autoAndSpeed`, `get_TalkTid`, `OnAction_ContinueTalk`, `EndTyping`).

## 사용자 확인
① 로그 Ready·예외 없음 ② 빠르게(오토 꺼짐·켜짐 각각): 문장 표시 후 약 3초 뒤 다음 대사, `[AutoHold] line … (auto=…) next line in 3 s` ③ 대기 중 CTRL → 즉시 진행, CTRL 누른 채로는 원본 속도 ④ 대기 중 클릭 → 즉시 진행, 한 줄 건너뜀 없음 ⑤ 느리게 모드 → 원본(오토 0.5초 / 수동) ⑥ 선택지 직전 대사: 선택지에서 멈추는지 ⑦ 컷신·폭풍 등 특수 대사 유형(8·9·13·14)에서 멈춤 없는지.
