# Text Speed 0.1.6 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 묶음 2). **기능 변경 없음.**
설치: `Mods\Additional_Functions\Restitutor_Additional_Textspeed.dll`, SHA256 `3AEF807822EAFF01C1799AE65CD44B71B95A7339C757D263A0A1AA530F7010B6`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `archive/`, 이전 DLL `releases/0.1.5/` (8488CBAA…).

## 바뀐 것
- 훅 7개(설정 화면 행 1 + 대화 6) 등록을 `HookSet.Hook` 으로. 처리기 타입이 둘(OptionsRow, TextSpeedModule)이라 같은 Harmony 인스턴스(`restitutor.module.textspeed.v1`)에 HookSet 2개. 중복 모듈 거부·설정 파일 경로·순서 그대로.
- 기존 찾기(Instance, DeclaredOnly, 이름 1개) → Core(Instance+Static, DeclaredOnly, 이름 1개): 메타데이터에서 7개 모두 1개(hookcheck).
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성.

## 검증
Release 빌드 경고/오류 0. 설정 저장 검사 48 통과. 게임 확인 전.
