# Rebalance NpcFleets 0.1.0 — NPC 쪽 대형 복선 제거 (2026-09-23)

상태: 구현됨-미확인. VM 빌드 경고/오류 0, 규칙 검사 PASS 87. DLL SHA256 `C7753325475A267D4EA960F05BCD8DDBA4417DFF08336FF62B79191B7EC31A3F`.
조사: 프로젝트 `handoff/BIG_FUCHUAN_REMOVAL.md` 5·6절. 플레이어 쪽은 Ship Editor 데이터(Rebalance Shipyard 모드는 2026-09-24 삭제).

## 사용자 결정
- NPC 배 구매 목록(GameConst `NPC_BUY_SHIP_LIST`, 41척)에서 대형 복선(300) 제거 → 40척.
- 대형 복선을 쓰던 NPC 함대: 해적 = 기함 간증선, 호위 자리는 해창선·개랑선 번갈아(고정). 교역 선단 = 복선. NPC 전용 사본(3290·3291·3293·3294, 원본과 수치 동일) 사용.
- 난이도 보충 없음. 새 DLL.

## 변경 (훅 2개, 표를 불러올 때만)
| 대상 | 종류 | 동작 |
|---|---|---|
| `TemplateManager.InitGameConst(ByteBuffer)` | Postfix | `_gameConst["NPC_BUY_SHIP_LIST"].value` 에서 `300` 토큰 제거 |
| `TemplateManager.InitNpcShipTeam(ByteBuffer)` | Postfix | `_npcShipTeam` 7행의 `flagShip`·`otherShips` 교체(행이 원본과 같을 때만, 다르면 그대로 두고 로그) |
- 설치 때도 1회 적용(표가 이미 불러와진 경우). 매 프레임·매 호출 처리 없음.

| 함대 | 전 | 후 |
|---|---|---|
| 9405011 연안 해적 함대 | 대형 복선 / 복선×4 | 간증선 / 복선×4 |
| 9504031 연안 해적 선단 | 대형 복선 / 대형 복선×2 | 간증선 / 해창선·개랑선 |
| 9505011 남해 반군 함대 | 대형 복선 / 대형 복선×2·관선×2 | 간증선 / 해창선·개랑선·관선×2 |
| 109979 해적 두목의 함대 | 대형 복선(2006) / 대형 복선·복선×2·광선 | 간증선 / 해창선·복선×2·광선 |
| 1300501 떠돌이 해적(이야기 일기토) | 대형 복선 / 복선 | 간증선 / 복선 |
| 1010089 진보회 함대 | 대형 복선 / 대형 복선·복선×3 | 복선 / 복선×4 |
| 1010098 서공 선단 | 마라호 / 유여호·복선×3 | 복선 / 복선×4 |

## 알려진 한계
- 세이브에 이미 만들어진 NPC 함대(돌아다니는 선단 등)는 세이브에 저장된 배를 유지 → 새 세이브 또는 함대가 새로 생길 때부터 적용 [추론: NpcShipTeamData 직렬화].
- NPC 가 이미 사 둔 대형 복선도 남음.
- 쓰이지 않는 함대 행 105·108(호위에 300)은 건드리지 않음.
- 해적 전투 난이도 하락(선원 175→100, 현측 6→4, 내구 1500→1350/1260/1230), 보상 그대로(사용자 결정).

## 되돌리기
`Mods\Rebalance\Restitutor_Rebalance_NpcFleets.dll` 삭제. 세이브에 남는 것: 이 모드로 만들어진 NPC 함대의 배(세이브에 저장된 함대).

## 테스트 (새 세이브)
① 로그 `Rebalance NpcFleets 0.1.0 loaded … 2 hooks` ② `GameConst (…): NPC_BUY_SHIP_LIST: removed 300 (41 -> 40 ships)` ③ `NpcShipTeam (…): 9405011 3300|… -> 3293|…` 7개 모두(`differs` 가 있으면 그 줄 전달) ④ 현상금 의뢰(연안 해적·남해 반군) 전투 또는 동아시아 해적 조우 → 적 함대 정보에 간증선·해창선·개랑선 ⑤ 진보회·서공 선단 정보에 복선 5척 ⑥ 오류 `[game const]`·`[npc ship team]` 없음
