# SailTrace 0.1.3 — 저장·날씨 시간 계측 추가(진단 전용)

2026-09-22. 0.1.2 + 시간 측정 훅 7개(모든 장면, 원본 동작 변경 없음) → `timed` 행(name, costMs, scene), `gap.inFrame.timed` 에 그 프레임에 돈 것 목록.
- 저장: `PlayerDataManager.SavePlayerData(4)`, `AutoSavePlayerData(1)`, `PlatformManager.SaveData(3)`(디스크 쓰기), `NpcAgentManager.SaveAllBehaviorsData()`
- 날씨: `WeatherMgr.PlayWeather(int)`, `WeatherController.ChangeWeather(WeatherEnum)`, `CloudController.OnTileCloudChange()`
배경: 5회차에 바다 530~540 ms 프레임 2건(한 프레임 IL2CPP +38~40 MB, 타일·배·월 갱신·GC 없음, 약 92초 간격). 날씨 자산 로드(코루틴, 비동기 부분은 시간에 안 잡힘)·자동 저장 후보를 시각으로 대조.
빌드: VM 경고 0·오류 0, 실행 확인 없음. SHA256 `4984f828b6408619762edc1c855a29ddd2c5a6f68f8ea884de92b4544183488e`.
