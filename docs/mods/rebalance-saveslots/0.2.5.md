# Rebalance SaveSlots 0.2.5 (2026-09-23)

0.2.4 게임 테스트(사용자, 15:00): **잘 동작** — 포커스 12번(`latest 12 -> row 12`), 숫자 클릭(`click 17/16/15/12`), Q/E(`Action_RB -> next page`, `Action_LB -> previous page`) 로그 확인. 요청: 페이지 줄을 **가운데 정렬**, 아래쪽 **사슬 테두리와 겹치지 않게 살짝 아래로**, 적당한 간격, **해상도가 바뀔 수 있으니 고정 수치 대신 비율·기준 객체로**.

## 근거 (UISystem 패키지 파일 분석, `StoragePanel` 2560×1440)
- 아래 사슬 테두리 = 자식 `n48`·`n20`·`n21`·`n49`, 그림 `ui_common_texture_10`(1440×18), y 1292 → 아래 끝 1310. 위 사슬은 y 132.
- 목록 `listStorage` (591,170,1922×1110) — 0.2.4 페이지 줄(880,1268)이 사슬(1292~1310)과 겹쳤음.
- 오른쪽 아래 조작 팁(`R 저장 파일 삭제`)은 다른 UI(UIOperationTips).

## 변경
- 위치를 숫자 대신 패널 안 객체로 계산, Refresh 때마다 다시 계산(크기 변화 따라감):
  - 가로: 부모(저장 패널) 폭의 가운데.
  - 세로: 사슬 테두리 아래 끝(`n20`, 없으면 `n21`)과 패널 아래 끝 사이 띠의 가운데. 띠가 페이지 줄보다 얇으면 사슬 바로 아래. 사슬을 못 찾으면 목록 아래 끝 기준.
- 로그 `storage pager: … chain n20 bottom …, pager (…)`.
- 이 파일 기준 예상 위치(2560×1440): 세로 1310~1440 띠 가운데 → 위 끝 약 1342 [계산], 가로 가운데.

## 테스트
1. 페이지 줄이 화면 가운데, 사슬 아래, 사슬·조작 팁과 겹치지 않음.
2. 창 크기/해상도를 바꿔도 같은 상대 위치.
