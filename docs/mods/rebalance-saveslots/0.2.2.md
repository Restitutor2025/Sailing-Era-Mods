# Rebalance SaveSlots 0.2.2 (2026-09-23)

0.2.1 게임 테스트(사용자, 13:23, 타이틀 → 불러오기): ① 최근 세이브로 포커스 안 됨 ② Q/E 안 먹힘(0.2.0 부터) ③ 키보드 S 가 두 칸씩 이동 ④ Q/E 아이콘 너무 작음 → 2.5배 ⑤ `<` `>` 는 글자가 아니라 화살표 이미지 ⑥ 화살표와 글자 사이 간격.

## 로그 근거 (13:23 Latest.log)
- 설치 DLL = 0.2.1(해시 6799C19C…), 머지 누락 아님 — 포커스 코드는 0.2.0 부터 DLL 에 들어 있음. 저장소: 0.2.0 은 PR #26 으로 main 병합, 0.2.1 커밋은 브랜치에만 있음(게임 동작과 무관).
- `storage hint: … arrows -/-` → 0.2.1 의 화살표(수량 창 버튼) 얻기 실패. 파일 분석 결과 그 버튼은 `CommonPrompt/btn_01` 이고 **제목 글자 '-' '+'** 라 원래 화살표가 아님.
- Q/E 처리 로그 줄 없음 → 모드 입력 처리기에 L1/R1 이 한 번도 안 옴. 원인 미확인.
- 포커스 결과 로그가 없어서 원인 구분 불가(0.2.0 인게임 저장 화면에서는 동작했음 — 스크린샷 11번 줄 = 번호 12).

## 변경
- 포커스: `ShowHook` 바로 뒤에도 적용(0.2.1 은 다음 Refresh 때만) + 다음 Refresh 에서 한 번 더. 열 때마다 로그 `focus (ShowHook|Refresh): used U, latest L, rows N -> row R; list selected S, model M.`
- `GetLatestStorageIndex` (0x9CE610) 확인: 101칸 전체에서 사용 중인 칸의 `timeOfStorage` 최댓값 → 대체값으로 맞음.
- 키 설명 줄: `[Q] < 5칸씩 이동 > [E]`, 저장 수 글자 가운데 아래. Q/E 아이콘 65×65(0.2.1 의 2.5배). 화살표 = 공용 패키지 `Common/ui_common_arrow_02`(22×30, 사용자 선택, 왼쪽은 좌우 반전). Common 은 게임 시작 때 항상 로드(`GameLaunch.InitCommonRes`). 요소 사이 간격: 키↔화살표 10, 화살표↔글자 16. 글자 폭은 자동.
- **임시 진단**: 화면이 열려 있는 동안 입력 이벤트 40개(`[diag] input f=… 이름 phase … button …`)·목록 이동 40개(`[diag] nav f=… list selected …`)·닫을 때 입력 수 합계. Q/E·S 원인 확인 후 제거.

## 테스트
1. 로그 `Rebalance SaveSlots 0.2.2 loaded … 9 hooks`.
2. 타이틀 → 불러오기: 최근 저장 칸에 포커스, `focus (…)` 줄.
3. 키 설명 줄 모양·크기·간격.
4. 불러오기 화면에서 Q, E, S 를 각각 한 번씩(천천히) 누르고 닫기 → 로그 `[diag]` 줄 전달.
