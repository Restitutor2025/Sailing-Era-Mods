# Rebalance SaveSlots 0.2.4 (2026-09-23)

0.2.3 게임 테스트(사용자, 14:49): 페이지 줄 표시(`pages 21, keys image/image, arrows image/image`), **마우스 클릭·Q/E 둘 다 동작 안 함**, 포커스는 맨 위.

## 로그 근거 (14:49 Latest.log, 0.2.2 진단)
- Q/E 는 모드에 도착: `Action_LB`(Q)·`Action_RB`(E) — phase **Started**(누름)·**Canceled**(뗌)만 오고 **Performed 는 없음**. 0.2.0~0.2.3 은 `c.performed` 를 조건으로 써서 한 번도 동작하지 않았음 [확인]. 같은 키로 `SailDown`/`SailUp` 도 같이 옴.
- 마우스: `Fire`·`SoloQTE` Started/Canceled 8회. 페이지 줄 클릭 처리 호출 여부는 로그 없음 [미확인] — 결과적으로 동작 안 함.
- 포커스: `used -1, latest 0 -> row 0`. 세이브 목록 파일(`PlayerDataHistory\0`, raw deflate) 직접 확인: 0번 자동 저장 `09/23/2026 13:20:01` 이 가장 최근, 직접 저장 최신은 12번(13:09) → 게임 `GetLatestStorageIndex` 가 자동 저장을 고른 것 [확인].

## 변경
- Q/E: phase `Started` 를 누름으로 처리(같은 프레임 중복 무시 유지).
- 페이지 줄 클릭: 글자·이미지 자체 대신 **항목마다 투명 불투명(opaque) GComponent 상자**(페이지 줄 높이 전체, 숫자는 좌우 여백 10)가 클릭을 받음 — Rebalance Growth 슬라이더 버튼과 같은 방식. 클릭마다 로그 `storage pager: click N`(열 때마다 아님, 실행당 20개).
- 포커스 대체값: **자동 저장(0·1) 제외, 직접 저장한 칸 중 `timeOfStorage` 최신**(사용자 결정). 없으면 0번. 이번 실행에서 저장·불러온 칸이 있으면 그 칸 우선(그대로).
- 진단(입력·목록 이동) 유지 — S 두 칸 이동 확인용.

## 테스트
1. 로그 `Rebalance SaveSlots 0.2.4 loaded … 10 hooks`, `focus (…): used -1, latest 12 …` (지금 세이브 기준 12).
2. Q/E 로 페이지 이동, 금색 번호가 따라감.
3. 숫자·화살표·키 아이콘 클릭 → 로그 `storage pager: click …` + 이동.
4. S 한 번 → `[diag] nav` 줄.
