# Item Rebuild 0.1.23 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 묶음 2). **기능 변경 없음.**
설치: `Mods\Additional_Functions\Restitutor_Additional_Item_Rebuild.dll`, SHA256 `7766E3DAF8E859558BA717AE8AE292571CA13A33D8C6E505F461E198F0787BCD`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `archive/`, 이전 DLL `releases/0.1.22/` (F73DD673…).

## 바뀐 것
- 설치 시 훅 48개(가방 14, 육지 탐험·수량 창 15, 판매 14, 보급 4, 해금 1) + 지연 훅 1개(UIManager.ShowInputNumPromptBox, 첫 수량 창 때) 등록을 모두 `Patch` → `HookSet.Hook` 으로. 기존 조건(DeclaredOnly, 이름 1개, 처리기 Static) 그대로, hookcheck 로 메타데이터 대상 전부 1개 확인.
- GameAssembly 해시 확인 → 훅 → enabled 순서 그대로 `InstallAll` 안에서. 실패 시 전체 해제 동일.
- UIManager 지연 훅은 Core 가드(게임 준비 전 UIManager 훅 거부)를 통과: 설치 시점이 게임 중이라 영향 없음.
- 검사: Core 소스를 함께 컴파일, 대역에 MelonEvents/SceneManager 추가, 준비 게이트 확인 1개 추가.
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성.

## 검증
Release 빌드 경고/오류 0. 검사 5676 통과(기존 5675 + Core 준비 게이트 1). 게임 확인 전.
