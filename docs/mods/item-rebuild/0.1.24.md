# Item Rebuild 0.1.24 (2026-09-26)

항로도 판별을 고정 번호(130001~130056)에서 **게임 표 PrefabLane.lineMap** 으로 바꿨다(사용자 규칙 2026-09-25 "하드코딩 절대 금지", 결정 2026-09-26 "같이 고침").
설치: `Mods\Additional_Functions\Restitutor_Additional_Item_Rebuild.dll`, SHA256 `0A5E80E4DB9A79A46DC9B2C50F91197AE550196E09289178E38B8BCDEDA43E0E`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 DLL `releases/0.1.23/` (7766E3DA…).

## 바뀐 것
- `Rules.AutoRoute(id)` = 어떤 PrefabLane 행의 lineMap 이 가리키는 아이템(`EntryPoint.LaneOf(id) > 0`). 번호 범위 없음 → City Editor 로 새로 만든 항로도(코어 newRow)도 같은 규칙(보유·해금 시 상점에서 숨김·구매 거부, 논리 용량 제외).
- `LaneOf` 지도는 lineMap≠0 인 모든 행으로 만든다. 표가 비어 있으면(템플릿 적재 전) 저장하지 않고 다음 호출에서 다시 읽는다(가방 용량 계산이 모든 행에 대해 묻게 되어서).
- 훅 수·대상 변경 없음. 새 interop 참조 없음(PrefabLaneValues·tid·lineMap 은 0.1.19부터 참조).

## 원본 동작과 달라지는 점
- 12007, 12039~12042(퀘스트 항로도 가능성으로 0.1.19에 제외했던 것)도 이제 항로도로 취급: 보유·해금 시 상점에서 숨김·구매 거부, **가방 논리 용량에서 제외**. 2026-09-25 모든 PropStore/College/Guild 행 확인 시 이 아이템들을 파는 곳은 없어 상점 동작은 원본 아이템 기준 그대로. 용량 제외는 새로 생기는 차이.

## 검증
VM 빌드(Release) 경고/오류 0. 검사 PASS 5636(0.1.23 5676 − 고정 범위 반복 검사 삭제 + 표 기반 검사·빈 표 미저장 검사 추가). 게임 확인 전.
