# Item Rebuild 0.1.22 — 판매 툴팁, 1개 즉시 선택, 수량 창 뒤 위치 복원

2026-09-22. 근거: `handoff/FEEDBACK_2026-09-22.md` 2·3절.

1. 툴팁 불일치: 원본 `UIPropStoreCtrl.SetCurGoods`(WASD/방향키 이동, Q/E 탭, SetStoreGoods)가 `ListCurPlay[ListPlayer.selectedIndex]`(원본 행)를 `_curGoods`(툴팁 대상)로 넣음 [본문]. 묶음 표시에서는 보이는 순번 ≠ 원본 순번. → SetCurGoods Postfix: 판매 행이면 `saleDisplay[selectedIndex].First` 로 교정, 보이는 행 수를 넘는 selectedIndex 는 마지막 행으로 제한. 목록이 새로 만들어져 아직 묶음 표시가 없으면 다음 Refresh 끝에서 1회 교정. 마우스 올림(OnRollOver, 버튼 data 사용)은 원래 정확해서 건드리지 않음.
2. 1개짜리: 수량 창 없이 클릭(또는 같은 종류 선택)마다 선택/해제 전환(사용자 결정).
3. 2개 이상: 수량 창을 열기 직전 판매 목록 selectedIndex·스크롤 위치(posX/posY) 저장 → 창이 닫힐 때와 그다음 Refresh 1회에 복원(일회성). 실제 증상(포인터/스크롤/선택 표시 중 무엇인지)은 미확정 — 게임 확인 필요.

폴링 없음: 모든 처리가 원본 함수 훅(SetCurGoods, OnClickBtnReturn, UIPropStoreView.Refresh) 안에서 이벤트 1회로 실행. 훅 49개(+SetCurGoods).
검증: PC VM 빌드 0/0, Tests PASS 5675(SaleTooltipTests 추가, ExpansionTests 는 1개 즉시 선택에 맞게 수정). verify_interop 미실행. 게임 확인 전.
DLL `ItemRebuild/evidence/0.1.22/` SHA256 f73dd673e617b098…c7eff3f3. 이전 소스 `ItemRebuild/archive/0.1.21-src/`.
