# Item Rebuild 0.1.18 — 언어 서적 실제 수량 통합

사용자 요청(2026-09-21): 가방 서적·지도 탭에서 언어를 배우는 서적만 묶음 정렬이 안 된다. 다른 아이템처럼 묶고 표기도 같게.
사용자 결정: ① 대상은 **언어를 가르치는 30종(71101~71130)만**. 71131~71145는 제외. ② 방식은 **스킬북(0.1.16)과 동일한 실제 병합**.

## 최종 동작과 범위

Rules.LanguageBook(71101~71130)을 추가하고 Rules.Book = SkillBook ∪ LanguageBook으로 묶었다. Rules.Stack, Rules.GroupedRecord, GroupableRecord가 SkillBook 대신 Book을 쓴다. 결과는 0.1.16 스킬북과 같다: 비무역 동일 ItemKey(ItemId/BuyPrice/Supply/Quality/IsSuper/state) 기록을 첫 기록 Number로 합산, 중복 기록·GUID 제거, 대표 GUID 1개 유지, 가방 한 칸 + x수량 배지, 논리 용량 한 칸, 판매 묶음 수량 선택, 학습 시 한 권 차감·마지막 권만 원본 삭제. 저장 행 병합은 되돌릴 수 없다(기존 inventory-backups 백업 경로 동일).

제외: 71131~71145(자연 풍경 화첩, 인문 조사 보고서, 생물 기록부, 고급 보물찾기 안내서, 여행기 11종). 게임 표상 type 73(언어 서적)이지만 SkillBook 표 kind=1로 스킬 116~120을 가르친다. 사용자 결정으로 계속 개별 표시.

신규 훅 없음(47개 유지).

## 근거

- [본문] 설치 table SHA256 `8be62de8…f48994`(= category-evidence 기준, 번들 `d64de774…` 재확인 2026-09-21). ItemType 표(chunk 85) tid 73 = code `book_language`, 이름 “언어 서적”, parentType 2000. 스크린샷의 “언어 서적-서적·지도”와 일치.
- [본문] Item 표(chunk 84) type 73 = 71101~71145 45행, 모두 pileNum 1.
- [본문] SkillBook 표(chunk 81) 필드 8(kind)/9(대상): 71101~71130 = kind 2, 대상 1~30(언어). 71131~71145 = kind 1, 대상 116~120(스킬). 71001~71100·71146~ = kind 1(기존 스킬북).
- [본문] UICharacterCtrl.OnClickBtnBook(evidence/0.1.15 asm): 언어 분기 0x94BECC PlayerRoleData.InnerAddLanguage → 0x94BED9 jmp 0x94BFF3 → 0x94C02E ItemBagData.InnerRemoveItemByGuid(책 GUID). 스킬 분기와 같은 삭제 호출이므로 기존 RemoveGuidBegin 수량 차감이 그대로 적용된다.
- [소스] Tab Characters BookPopup.cs 350행: 책 수량을 BookGuid+ItemId+비무역 기록의 Number로 읽음 → 병합된 언어 서적도 xN 표시.

## 검증과 제한

오프라인 검사 추가(tests/LanguageBookTests.cs): 30종 Book/Stack/GroupedRecord, 71131~71145 비대상, 스킬북 경계 비중첩, 성경 3권 병합·다른 언어책/무역/71131 보존, 한 칸 점유, x3 배지, 학습 한 권 차감. 사용자 PC 결과(2026-09-21): Release 빌드 성공. build.ps1은 2단계에서 `evidence/managed-tests.txt` 쓰기 거부(파일 소유자 `RESTITUTOR\CodexSandboxOffline`, icacls 부여 후에도 거부 — 원인 미확정)로 중단되어 **verify.py·verify_interop.ps1·루트 복사는 실행되지 않았다**. 사용자가 `dotnet run`으로 검사를 직접 실행: **PASS 5563**(0.1.16 기록 5512). 과거 저장 사본 389→**239**행(0.1.16 기록 251 → 언어 서적 병합으로 12행 추가 감소), 모든 ItemId 총수량 보존. 결과 사본 `ItemRebuild/evidence/0.1.18/managed-tests.txt`.
DLL SHA256(bin/Release) `8cca32d9fac733795a80d0fc27560e1f481e68119f5346379ed400948ffe0aea`. 루트 DLL은 0.1.16(`44E9366C…`) 그대로. 게임·Steam 실행 없음, 자동 설치 없음.

실전 확인 항목: 서적·지도 탭에서 같은 언어 서적 한 칸 xN, 인물 창 언어 학습 후 N-1, 마지막 권 삭제, 판매 수량 선택, 저장·재로드 후 유지, 71131~71145는 기존처럼 개별 표시.

## 사용자 실전 보고 (2026-09-21)

bin/Release DLL(`8CCA32D9…`)을 설치해 게임에서 확인, "완료 잘 적용돼". 세부 항목별(차감·재로드·71131~71145 분리) 개별 보고는 없음. 상태: **완료**.
