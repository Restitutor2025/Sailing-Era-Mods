# Item Rebuild — 0.1.23 (Restitutor.Core 전환, 기능 변경 없음, 게임 확인 전)
[0.1.23](0.1.23.md). 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.

이전 기록:

# Item Rebuild — 0.1.22

판매 툴팁을 묶음 행에 맞춤(SetCurGoods Postfix), 1개짜리는 수량 창 없이 선택, 수량 창 뒤 목록 위치 복원. 빌드 0/0, PASS 5675, 게임 확인 전. [상세](0.1.22.md)

# Item Rebuild — 0.1.21

항로도 자동 사용과 0.1.20 해금 안내창·지식 기록·보충을 모두 롤백했다(0.1.20에서 안내창을 닫은 뒤 상점 화면·입력이 복구되지 않음 — 사용자 보고). 항로도는 가방에서 직접 사용(원본)하고, 보유 중이거나 이미 해금된 항로의 항로도(130001~130056)는 상점에서 숨기고 구매를 막는 것만 남긴다. 선실 설계도 규칙은 0.1.19 그대로.

훅 48개(0.1.20의 2개 제거). 빌드 경고/오류 0, PASS 5659, 48 훅·275 참조 interop 통과. 루트 DLL SHA256 76DEBA69…. Mods 설치(해시 일치), 2026-09-22 사용자 게임 내 확인 완료. [버전·근거](0.1.21.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.20

구매로 자동 사용된 항로도(130001~130056)에 원본 해금 안내창(UIOpenLineCtrl.ShowOpenLine)을 띄운다. 여러 장이면 창을 닫을 때마다 다음 항로 창을 차례로 띄운다. 원본 안내창 함수가 하던 지식 항로도 기록(KnowledgeDB.AddRouteMapData)을 구매 즉시 남기고, 0.1.19로 빠진 기록은 세이브 로드 때 한 번 보충한다.

신규 훅 2개(KnowledgeDB·PlayerLaneDB Deserialize Postfix, 총 50). 빌드 경고/오류 0, PASS 5671, 50 훅·283 참조 interop 통과(Claude PC 작업 VM). 루트 DLL SHA256 EADE714F…. Mods 설치·실전 확인 전. [버전·근거](0.1.20.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.19

선실 설계도 12종·선실 확장 공법(11000~11010, 11036, 11037)을 가방에서 숨긴다(보유가 해금 조건이라 행은 유지, 용량 제외). 항로도 130001~130056은 구매 즉시 원본 방식으로 사용(항로 해금·아이템 삭제, 추가 연출 없음). 보유 중인 선실 아이템과 해금된 항로도는 상점에서 숨기고 구매도 막는다.

신규 훅 1개(UIPropStoreCtrl.SetStoreGoods, 총 48). 빌드 경고/오류 0, 오프라인 검사 PASS 5658, 48 훅·277 참조 interop 대조 통과(Claude PC 작업 VM 빌드). 루트 DLL SHA256 4DDC19ED…. Mods 설치·실전 확인 전. [버전·근거](0.1.19.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.18

언어를 가르치는 언어 서적 30종(71101~71130)을 스킬북과 같은 실제 수량 통합 대상에 넣었다. 가방 한 칸 x수량, 논리 용량·판매 묶음, 한 권씩 소비. 같은 type 73이지만 스킬을 가르치는 71131~71145는 제외(사용자 결정).

신규 훅 없음. 사용자 PC 빌드 성공, 오프라인 검사 PASS 5563. build.ps1은 결과 파일 권한 오류로 중단되어 네이티브/interop 검증·루트 복사는 미실행. **2026-09-21 사용자 실전 보고: 설치 후 "잘 적용됨" — 완료.** [버전·근거](0.1.18.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.17

가방에서 의상-장비(type 33) 59종을 무기/방어구/도구와 같은 장비 묶음 규칙으로 표시한다. 미장착·비무역·동일 속성 기록은 한 칸 x수량이다. 장착 중·다른 속성 기록은 분리된다. 구귀법(10111)은 포함, 무역 통계 수첩 9종(33062~33070)은 제외(사용자 결정). 표시·용량·판매 선택만 바뀌며 저장 행/GUID는 보존한다.

신규 훅 없음. 빌드·검사 결과는 사용자 build.ps1 실행 전. 자동 설치 및 게임·Steam 실행 없음. [버전·근거](0.1.17.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.16

스킬북 108종의 비무역 동일 종류·속성 기록을 실제 Number 수량으로 통합한다. 권별 중복 기록/GUID 제거, 원본 저장·소비 연결용 묶음 대표 GUID 하나 유지. 한 권씩 소비하고 마지막 권만 기록 삭제. 다른 속성·언어책·장비는 보존한다.

Release 경고/오류 0, 오프라인 5,472개 검사, 기존 47개 훅/266개 참조 검사 통과. 루트 기존 DLL 갱신, 자동 설치 및 게임·Steam 실행 없음. 실전 검증 전. [버전·근거](0.1.16.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.15

스킬북(type 72) 108종을 같은 ItemId/속성끼리 가방에서 대표 아이콘과 x수량으로 묶고 논리 가방 용량을 묶음당 한 칸으로 계산한다. 판매도 기존 묶음 수량 선택/가운데 최대 선택을 적용한다. 일반 서적·지도·언어책은 제외한다.

원본 스킬 습득은 책 GUID로 InnerRemoveItemByGuid를 호출하므로 개별 저장 행과 GUID를 보존한다. 실제 수량 병합은 하지 않는다. Release 경고/오류 0, 오프라인 5,449개 검사, 기존 47개 훅/266개 interop 참조 및 시작 그래프 통과. 기존 DLL만 갱신, 새 DLL/자동 설치 없음. 게임·Steam 실행 및 실전 검증 없음. [근거·제한](0.1.15.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.14

역참 보급품 아이콘(중앙 보충 버튼과 왼쪽 가방)을 가운데 클릭하면 수량 창 없이 현재 도구 무게를 제외한 최대 보급품 수량으로 채운다. 원본 ShowHook의 MaxNum 계산을 재사용하며 이미 담긴 보급품 무게를 중복 차감하지 않는다. 반복 클릭은 같은 최대 수량을 유지한다.

왼쪽 휴대 도구에 별도 x수량 표시를 추가했다. 보급품 전용 controller와 분리하고 기존 하단 무게는 유지한다. 재사용/화면 이탈 시 표시와 이벤트를 정리한다.

기존 DLL 0.1.14 갱신, 새 배포 DLL/자동 설치 없음. Release 경고/오류 0, 오프라인 5,323개 검사, 47개 고유 훅/266개 interop 참조/시작 그래프 통과. 게임·Steam 실행 및 실전 검증 없음. [근거·제한](0.1.14.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.13

판매/탐험 수량 창 슬라이더 양쪽에 Min(0)·Max(현재 상한) 버튼을 추가했다. 보급품의 원본 최소/최대 버튼은 유지한다. 상품/탐험 도구를 가운데 클릭하면 수량 창 없이 최대 수량을 적용한다. 판매는 해당 묶음의 판매 가능량, 구매는 현재 상점 동일 ItemId 전체 재고를 선택하며 최종 거래 확정은 원본 절차다. 탐험은 재고·무게가 허용하면 3개를 즉시 휴대 물자에 넣는다. 반복 클릭은 해제가 아니라 Max 유지다.

기존 가운데 버튼의 R2/긴 누름 L2와 중복 실행되지 않도록 물리 가운데 버튼 입력일 때만 원본 경로를 억제한다. 재사용 아이콘/화면 이탈/종료에 이벤트를 갱신·정리한다.

기존 DLL 0.1.13 갱신, 새 배포 DLL/자동 설치 없음. 이미 설치된 UnityEngine.InputLegacyModule을 읽기 참조(Private=false)한다. Release 경고/오류 0, 오프라인 5,307개 검사, 46개 고유 native 훅/257개 interop 참조/시작 그래프 검사 통과. 게임·Steam 실행 및 실전 검증 없음. [근거·제한](0.1.13.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.12

사용자 화면에서 무기/방어구/도구 장비 묶음 실패와 기념품 누락을 확인했다. 장비 원본 Deserialize가 별도 PlayerItemData 객체를 만드는 본문을 재확인하고, Unworn의 객체 주소 일치 조건을 GUID+ItemId 일치로 수정했다. 장착/효과/속성 구분 및 개별 GUID 보존은 유지한다. 이 공통 판정은 가방 표시·논리 용량·판매 묶음에 적용된다.

설치 Item type=4 기념품 41종을 동일 속성의 실제 소모품 묶음에 추가했다. 기존 61종과 합계 102종이며 역참 13종 정책은 별도다. 한 개라도 남으면 한 칸, 전량 제거 후 0칸이다.

기존 DLL 0.1.12 갱신, 신규 의존 DLL/자동 설치 없음. Release 경고/오류 0, 오프라인 5,266개 검사, 고유 훅 44개/interop 참조 254개/시작 그래프 검사 통과. 게임·Steam 실행 및 이번 버전 실전 검증 없음. [근거·제한](0.1.12.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.11

보급품 마우스 조작 영역을 원본 ± 버튼 사이의 보이는 바 위치에 맞췄다. 판매/탐험 수량 슬라이더는 원본 입력 필드와 하단 버튼 사이 팝업 내부에 배치한다. 자체 숫자 창의 제목은 수량 선택이며 ±는 0↔현재 최대값으로 순환한다. 탐험 최대 3개/재고/무게 제한은 유지한다.

판매·탐험·보급품에 `(ESC) 취소`, `(Space) 적용` 두 마우스 버튼을 원본 하단 버튼 중심에 대칭 배치한다. 각 버튼 45%, 간격 10%. ESC의 원본 감소 동작은 자체 숫자 팝업에서 취소로 교체하고 적용 때만 확정 콜백을 호출한다. 상품 수량 글자는 18→27, 영역 높이는 24→36이다.

기존 DLL 0.1.11 갱신, 새 의존 DLL/자동 설치 없음. Release 경고/오류 0, 오프라인 5,177개 검사, 고유 native 훅 44개/interop 참조 254개/시작 호출 그래프 검사 통과. 사용자 0.1.10 실전 실패 보고를 수정한 버전이며 이번 버전의 실제 화면·클릭은 미검증이다. 게임·Steam 실행 없음. [근거·제한](0.1.11.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.10

판매 목록도 동일 속성/단가/판매 조건별 대표 아이콘 하나로 묶는다. 수량은 x보유량 또는 선택량/보유량이며 가격은 원래 단가를 유지한다. 실제 단위 Slot/GUID는 결제용으로 보존한다. 전체/같은 상품 선택도 표시 인덱스를 실제 항목에 대응하도록 수정했다. 상점의 용량 표시에는 0.1.9의 논리 점유량을 적용한다.

판매·역참 탐험 도구·역참 보급품 수량에 마우스 클릭/드래그를 제공한다. 클릭 위치에 가장 가까운 정수, 정확한 중간은 큰 쪽으로 반올림한다. 판매 0~현재 판매 가능량, 탐험은 재고/3개/무게 상한, 보급품은 원본 MaxNum과 가격 계산을 따른다.

기존 DLL 0.1.10 갱신, 별도 DLL/자동 설치 없음. Release 경고/오류 0, 오프라인 대역 5,155개 검사, 고유 native 훅 40개/interop 참조 238개/시작 호출 그래프 검사 통과. 게임·Steam 실행 및 이번 버전 실전 검증 없음. [상세 근거·제한](0.1.10.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.9

장비 묶음도 실제 가방 용량을 한 칸으로 계산한다. 동일 속성의 미장착 장비가 하나라도 남으면 한 칸, 전량 제거 시 0칸이다. GUID/장비 상태/저장 행은 보존하며 가방 표시·빈 공간 조회·획득·동시 구매/판매 검사에 논리 점유량을 적용한다. 같은 묶음의 추가 획득/구매는 새 칸을 요구하지 않는다. 장착 장비와 다른 속성은 별도 칸이다. 0.1.8의 여섯 분류 지원 및 판매 슬라이더 min=0/max=판매 가능량을 유지한다.

기존 이름의 루트 DLL만 갱신했고 자동 설치하지 않았다. Release 경고/오류 0, 오프라인 3,943개 검사, 고유 native 훅 34개·참조 219개·시작 지연 초기화 검사 통과. 저장 사본 389→280행, 종류별 수량 보존. 게임/Steam 실행과 실전 검증은 하지 않았다. [원본 근거·범위·제한](0.1.9.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.8

기존 DLL 안에 여섯 분류 지원과 판매 수량 슬라이더를 추가했다. 보조 선박 자재/항해 도구/일반 소모품 61종은 동일 속성끼리 실제 수량 병합한다. 무기/방어구/도구 장비 102종은 미장착 동일 속성만 가방에서 표시를 묶고 개별 GUID/저장 행을 보존한다. 장비의 실제 용량 점유는 개별 행 그대로다. 역참 13종의 최대 3개·무게 정책은 별도 유지한다.

판매 팝업은 min=0, max=현재 판매 가능 개수, 정수 슬라이더/±1/직접 입력이며 0은 선택 해제다. 원본 단위 Slot에 선택 수량을 대응시켜 원본 가격·결제를 유지한다. 소모품 묶음이 하나라도 남으면 한 칸을 점유하고 전량 판매 때만 한 칸을 비운다. 동시 구매 공간 부족은 결제 전 안내한다. 상점 목록 자체와 NPC 물물교환 UI는 기존대로다.

Release 경고/오류 0, 오프라인 대역 3,880개 검사, native 훅 31개·interop 참조 208개·지연 초기화 검사 통과. 보존 저장 사본 389→280행, 종류별 총수량 보존. 루트 DLL 갱신, 자동 설치 없음. 게임·Steam 실행 및 이번 버전 실전 확인 없음. [정책·원본 근거·제한](0.1.8.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.7

0.1.7: 가방 수량 표시의 아이콘 내부 여백을 4→14 UI 단위(작은 아이콘에서는 짧은 변의 20% 상한)로 늘렸다. 흰색 글자·검은색 외곽선 1을 적용하고 기존 글자 크기에서 10% 확대(정수 반올림)했다. 기존 우측/하단 정렬, 긴 수량 Shrink 및 변경 시 갱신을 유지한다. 실전 화면 확인 전이다. [근거](0.1.7.md).

아래는 이전 버전 기록이다.

# Item Rebuild — 0.1.6

0.1.6: 사용자 이미지에서 0.1.5 수량 위치 실패 및 역참 도구 분산 확인. 배지 위치를 Stage 변환 대신 loaderIcon과 같은 부모의 xMin/yMin/actualWidth/actualHeight로 계산하고 sortingOrder를 최상위로 지정했다. 템플릿 tid와 실제 항목 ID 불일치 시 잘못된 배지 생성을 거부한다. RefreshDataModel 전에 도구를 최초 도구 위치에 연속 배치하며 도구/다른 항목 각각의 상대 순서를 보존한다. 변경 기반 갱신 유지, 폴링 없음. [0.1.6 근거](0.1.6.md). 실전 수정 효과 확인 전이다.

DLL: `Restitutor_Additional_Item_Rebuild.dll`. 2026-09-20 역참 클릭 무반응/빈 칸 예외 수정본, 루트 DLL 제공, 자동 설치 없음.

0.1.1 사용자 로그는 시작 화면 진입·세이브 병합·역참 UI까지 진행했다. 역참의 CarryLabel 반복 오류와 클릭 시 IndexOf→Contains→ChooseLand NullReferenceException 확인. 원본 ShowHook이 ListLandBag[0]에 null 보급품 칸을 넣음을 본문에서 확인하고 검색/표시/출발 변환 전체에 반영했다. 해당 조건에서 수정 전 예외를 대역으로 재현했고 수정 후 통과했다. 0.1.2 실전 클릭·팝업 확인 전이다.

사용자는 0.1.0 설치 시 타이틀 이전 검은 화면, 제거 시 정상이라고 보고했다. Unity Player-prev.log에서 FairyGUI 초기화 및 UIManager.StartUp NullReferenceException 확인. UIManager 패치 등록이 native .cctor의 Stage.Instantiate를 조기 실행하는 경로를 확인하고 해당 1개 훅을 최초 역참 수량 팝업까지 지연했다. 나머지 21개는 기존 등록 시점 유지. 수정 후 실전 성공은 사용자 확인 전이다.

역참 도구 13종의 플레이어 보유 데이터를 종류+수량으로 통합. 대표 GUID 및 기존 스키마 유지, 중복 행과 도구별 품질 등 개별 차이 제거. 책·장비·무역품 제외. 가방 수량 표시 및 출발 준비 숫자 팝업(±1/직접 입력/종류별 최대 3/재고·무게 제한/0 해제) 구현. 상점·NPC 교환의 원래 개수별 선택 UI는 유지한다.

Release 경고/오류 0, 실제 소스를 사용한 오프라인 대역 3,668개 검사 통과. 시작 호출 그래프에서 UIManager 접근/지연 함수 도달 없음 확인. 고유 native hook 24개/동일 RVA 별칭 없음, 설치 interop 서명 및 참조 129개 검사 통과. 과거 저장 사본 389→308행, 모든 종류 총수량 보존. 게임·Steam 실행 없음. 실전 화면, 출발/귀환·저장·판매/선물과 병용 검증은 사용자 확인 전이다. 성능 개선 수치 미측정.

[사용법](../../../ItemRebuild/README.md) · [패치](PATCHES.md) · [클릭 오류 수정](0.1.2.md) · [초기화 수정](0.1.1.md) · [최초 구현](0.1.0.md)
