# Devil Fruits 0.1.6 — 2026-09-22 작성 / 2026-09-23 빌드·설치 (게임 확인 전)

빌드 결과: 경고·오류 0, 규칙 검사 PASS 10, `Mods/Rebalance/Restitutor_devil_fruits.dll` SHA256 `B9069A1906980ABDC36F541C6A0C3E02D9A4650AB2CBA741C0D2437D5DAE34FF`. 되돌리기: `DevilFruits/releases/0.1.5/` DLL 덮어쓰기.

## 사용자 요청 (스크린샷, 인물 창 속성 열)
등급 글자에 올리고 좌클릭하는 방식 대신, **등급 글자 바로 옆(글자와 수치 사이 빈 자리)에 작은 과일 아이콘**을 두고 그 아이콘에 **마우스를 올리면** 열매 창이 뜨게 분리.

## 사용자 결정
- 열기/닫기: 아이콘에 올리면 뜨고, 커서가 아이콘·패널을 모두 벗어나면 닫힘.
- 아이콘 표시: **사용 가능할 때만**(해당 열매 보유 1개 이상이고 등급이 S가 아님).

## 변경 (0.1.5 기준, 0.1.5의 Restitutor.Core·OnClickBtnLevelUpFive 재동기화 유지)
- 등급 글자 클릭 가로채기 제거: `UISheetCharacter.btnReturn` onTouchBegin/End 리스너를 더 이상 붙이지 않음. 등급 글자는 Stat Rank 본래 동작(호버 팁)만.
- 아이콘: 각 등급 글자와 같은 부모에 `MyGLoader`(이름 `RestitutorDevilFruitIcon`, touchable=false, 아이템 icon = 대추야자), 크기 = 글자 높이×1.15, 위치 = 글자 오른쪽 + 글자 높이×0.25, 세로 가운데. 10프레임마다 보유 수·등급으로 표시/숨김 갱신(치트 지급·사용 직후도 반영). 인물 창 닫힘(HideHook)·다른 창 전환 시 폐기.
- 호버: Stage 커서 위치로 판정(btnReturn이 이 열을 덮어 rollOver가 오지 않음 — Stat Rank와 같은 방식). 여는 조건 = 커서가 아이콘 안 + 현재 hit 대상이 btnReturn(다른 패널이 위에 있으면 안 열림). 유지 = 커서가 패널 또는 그 아이콘 안(여유 4px).
- 패널: 오른쪽 끝이 아이콘 왼쪽 끝에 닿게(틈 0), 세로 가운데 — 커서를 아이콘에서 왼쪽으로 옮기면 바로 패널 안. 왼쪽 공간이 없으면 아이콘 오른쪽. 전체 화면 차단막 제거(패널 밖 입력은 원본으로). 내용·alpha 0.8·[사용] 동작은 0.1.1과 같음. 사용 후 아이콘 갱신 → 같은 스탯 패널 다시 표시(사용 불가가 되면 이유 문구 표시, 커서가 떠나면 닫힘).
- 가방 설명(ItemType bagHint) 문구: "*인물 화면에서 능력 등급 옆 열매 아이콘에 마우스를 올려 사용".
- 훅 변화 없음. 이전 소스: `DevilFruits/archive/0.1.5/`.

## 사용자 확인
① 열매 보유·S 아님인 스탯에만 아이콘 ② 아이콘에 올리면 패널, 패널로 옮겨 [사용] 가능 ③ 아이콘·패널을 벗어나면 닫힘 ④ 등급 글자 호버 팁은 그대로, 클릭은 원본 ⑤ 마지막 열매 사용 후 아이콘 사라짐 ⑥ 아이콘이 수치(57 등)나 막대와 겹치지 않는지.
