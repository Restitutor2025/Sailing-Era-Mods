# Devil Fruits 0.1.3 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 묶음 1). **기능 변경 없음.**
설치: `Mods\Rebalance\Restitutor_devil_fruits.dll`, SHA256 `826CD7F93A22B2E4E83EA78C66EDA2128191D875A671AF6EC455479CD2B1F22C`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `DevilFruits/archive/0.1.2/EntryPoint.cs`, 이전 DLL `DevilFruits/releases/0.1.2/` (1516690B…).

## 바뀐 것
- 훅 11개 등록을 `HookSet.Hook` 으로. 기존 찾기(DeclaredOnly, 이름 + 인자 개수, 첫 번째 것)를 이름만(인자 무관)으로 바꿨지만, 실제 interop 메타데이터에서 11개 이름 모두 해당 타입에 1개뿐이고 인자 개수도 기존 조건과 같음(hookcheck) → 같은 메서드. 순서·처리기 동일.
- 해시 확인 → 훅 11개 → enabled → EnsureTemplates("init") 순서 그대로 `InstallAll` 안에서. 실패 시 전체 해제 + enabled=false 동일.
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성.

## 검증
Release 빌드 경고/오류 0. 규칙 검사 10개 통과(기존). 등록 대상 메타데이터 확인 11/11. 게임 확인 전.
