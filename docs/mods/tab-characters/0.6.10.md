# Tab Characters 0.6.10 — 우클릭 누른 동안 입력 전체 차단 수정

2026-09-22. 사용자 제보 + 재현 확인: 책/장비 패널을 한 번 닫은 뒤에는 우클릭을 누르고 있는 동안 E/Q/Alt 등 모든 게임 입력이 무시됨.

원인(0.6.9): `closeInputUntil` 프레임 창. 패널을 닫으면 0 이상이 되고 ResetBooks 전까지 유지, TickBooks 가 Esc/우클릭을 누르는 매 프레임 창을 연장, BookCapture(OnEventCaptureInput Prefix)가 창 안의 **모든 액션**을 차단.

수정: 프레임 창 폐지. 패널을 닫은 물리 키(우클릭/Esc)를 `closeKey` 로 기억하고, BookCapture 안에서(입력 이벤트가 올 때만) 그 키가 눌려 있는 동안의 `Action_B` 와 그 키를 뗄 때의 `Action_B` 해제 1회만 삼킨다. 다른 액션은 통과. InputAction 경로로 닫은 경우는 기존 bookHeldActions(떼어질 때까지)만 사용. 매 프레임 우클릭/Esc 누름 확인(160행) 삭제.

검증: PC VM 빌드 0/0. Tests PASS 11511, PopupTests PASS 3224(회귀 검사 9개 추가: 닫은 우클릭 유지 중 다른 액션 통과, 자기 Action_B 유지·해제 1회 차단, 이후 우클릭 유지 중 SailUp/Search/Action_B 통과). 훅 변경 없음. verify_interop 미실행(pwsh 없음). 게임 확인 전.
DLL `evidence/0.6.10/Restitutor_Additional_Tab_Characters.dll` SHA256 e48acd9453ba9825…14bbaacc. 이전 소스 `TabCharacters/archive/0.6.9-src/`.
