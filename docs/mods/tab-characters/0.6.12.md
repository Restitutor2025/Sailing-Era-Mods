# Tab Characters 0.6.12 (2026-09-22)

CORE_PLAN 5단계(입력 공통 처리) 묶음 3. **기능 변경 없음**(입력 판정 로직 동일).
설치: `Mods\Additional_Functions\Restitutor_Additional_Tab_Characters.dll`, SHA256 `0F9C4A6DD38EA19CA495B1FE9676B8FDC9A19F13374403994F2140DD7B1CF411`. 이전: 0.6.11 (0BDAF1ED…).

## 바뀐 것
- 훅 등록을 `HookSet.Hook(..., args: 정확한 인자)` 로(기존 GetMethod 정확 서명과 같음).
- 입력: 자체 `InputSystemManager.OnEventCaptureInput` Prefix 를 없애고 Core 공용 입력 게이트(`HookSet.Input`)에 같은 처리기를 등록. 판정 로직(무엇을 막는지)은 그대로. 게이트는 등록된 처리기를 모두 실행하고 하나라도 false 면 입력을 막음(= 여러 Prefix 가 따로 있던 때와 같음). 처리기에서 예외가 나면 1회 기록 후 "허용"으로 처리(이전엔 예외가 원본 호출 쪽으로 전파될 수 있었음). 대상: BookCapture(우클릭 닫기 키 삼키기·책/장비 패널 키 처리).
- 복구·종료 경로의 `HarmonyInstance.UnpatchSelf()` 4곳 → `RemoveHooks()`(Harmony 훅 + 입력 처리기 같이 제거). 필요 Core 0.2.0.

## 검증
Release 빌드 경고/오류 0. 검사 11511 + 팝업 3224 통과. 훅 대상 메타데이터 확인. 게임 확인 전.
