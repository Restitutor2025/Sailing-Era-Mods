# Tab Characters 0.6.11 — 대기 중 키 읽기 제거(기능 동일)

게임 확인 전. [상세](0.6.11.md)

# Tab Characters 0.6.10 — 우클릭 입력 차단 수정

책/장비 패널을 닫은 뒤 우클릭 누른 동안 모든 입력이 막히던 0.6.9 결함 수정. 닫은 키의 Action_B 만 떼어질 때까지 삼킴. 빌드 0/0, Tests 11511·PopupTests 3224, 게임 확인 전. [상세](0.6.10.md)

# Tab Characters — 0.6.9

[0.6.8 로그 22:58] 언어 줄 클릭 수신은 성공, 그러나 언어 서적 상세에서 원본 `RefreshSkillBookTips` NRE. 원인: 원본이 `roleInfo.listLanguage.data`(영웅 언어 목록)를 스킬 시트(`_sheetType==1`)에서만 채움 — 스킬 탭 폐지 후 null. 수정: 그 호출 동안만 data·항목 수를 원본과 같이 준비하고 복원. 빌드·화면 확인 전. [근거](0.6.9.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.8

[0.6.7 진단 로그 22:55] 언어 제목·글자 누름 3회 모두 `btnReturn`(화면 전체 GGraph)이 받았고 클릭 핸들러 호출 0회(연결은 부착됨). 수정: 장비 칸과 같은 방식으로 btnReturn에서 언어 영역 누름만 잡아 원본 클릭을 취소하고 언어 학습 패널을 연다. 훅 26 유지. 빌드·화면 확인 전. [근거](0.6.8.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.7 진단본

[사용자 보고] 인물 탭 언어 줄 클릭 시 언어 학습 패널이 뜨지 않음(설치 0.6.5). 언어 경로에 로그가 없어 원인 후보(누름 가로챔 / 연결 미부착 / 조기 반환)를 구별할 수 없어 `[LangTrace]` 계측만 추가. 동작 변경 없음. 0.6.6 버튼 변경 포함. 빌드·화면 확인 전. [근거](0.6.7.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.6

[사용자 보고] 0.6.5 목록 우클릭 해제 1회로 동작(22:07 로그). [사용자 요청] 학습 패널의 `스킬 레벨 +1` 버튼이 잘 안 보임 → 높이 52→84, 글자 23→38 가운데 정렬, 흰 글자·외곽선, 파란 채움·밝은 테두리(누를 수 없을 때는 회색·작은 글자). 동작 경로 변경 없음. 빌드·화면 확인 전. [근거](0.6.6.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.5

[사용자 보고] 다른 항해사 장비를 목록에서 우클릭으로 벗기려면 두 번 눌러야 함. 원인: 장착 변경 뒤 게임이 한 프레임 이상 늦게 모델 목록을 묶지 않은 원래 목록으로 다시 만들어, 화면 행 번호와 모델 목록이 어긋남(다음 클릭이 다른 기록을 가리킴). 수정: 목록을 쓰기 전 교체 여부를 확인해 다시 묶고, 행 번호는 화면에 그린 목록의 GUID로 현재 목록 위치를 찾아 쓴다. 교체 감지 로그 추가. 빌드·화면 확인 전. [근거](0.6.5.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.4

[사용자 보고] 0.6.3 장비 패널 정상 작동, 단 닫기 버튼에 글자가 보이지 않음. 수정: 닫기 글자를 책 패널 라벨과 같은 방식(자동 크기)으로 만들고 버튼 가운데 배치. 상단 장비 탭 폐지(스킬 탭과 같은 방식): 버튼 숨김·자리 접기, L2/R2·클릭·P 진입 차단, 장비 시트 요청은 인물 시트로. 훅 수 26 유지. 빌드·화면 확인 전. [근거](0.6.4.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.3

장비 패널을 오른쪽으로 1.25배(약 750), 위로 2배(아래쪽은 장비 칸에 고정) 키우고 목록 아이콘도 1.25배. 착용하지 않은 같은 장비(ItemId·구매가·보급·품질·특급·상태가 같고 효과 GUID 없음)를 Item Rebuild 0.1.17 가방 규칙대로 한 칸 `xN`으로 묶음. 착용 중인 장비는 따로. 표시 전용(GUID·세이브 불변). 빌드·화면 확인 전. [근거](0.6.3.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.2

0.6.1 계측 결과: 장비 칸 위 좌클릭 11회 모두 `UISheetCharacter.btnReturn`(GGraph)이 받았다. 칸 바인딩은 붙어 있었고(칸 4개 touchable) OpenEquip은 한 번도 호출되지 않았다. 수정: btnReturn에 누름/뗌 리스너를 추가해 좌표가 장비 칸 안일 때만 원본 클릭을 취소하고 패널을 연다. 칸 밖 누름은 원본 그대로. 계측 유지(좌클릭 탐지는 3회로 축소). 빌드·화면 확인 전. [근거](0.6.2.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.1 진단본

0.6.0 실행(20:51)에서 v0.6.0 로드·오류 없음, 그러나 장비 칸 클릭에 반응 없음(사용자 보고) 그리고 `equipment panel opened` 로그 없음. 원인 후보(바인딩 미부착 / 다른 객체가 클릭을 가로챔 / OpenEquip 조기 반환)를 로그로 구별할 수 없어 `[EquipTrace]` 계측만 추가. 동작 변경 없음. [근거](0.6.1.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.6.0

인물 창 장비 칸을 클릭하면 원본 장비 목록(4열)·필터·설명이 칸 옆 말풍선 패널로 뜬다. 마우스를 올리면 스킬 `+N`(인물 창 행과 패널 옆 상자)과 HP·공격 변화를 미리 본다. 노란 선택 칸 위 우클릭=그 칸 해제, 목록 항목 우클릭=원본 해제, 그 밖=닫기, Space=원본 장착/해제. 장착·해제·판정·안내는 원본 함수를 SheetType 2 범위에서 호출. 새 훅 1개(총 26). **빌드·화면 확인 전.** [근거](0.6.0.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.10

책을 바꿀 때 학습 패널이 원본 정보창에서 멀어지던 문제 수정: 위치 기준을 연 순간 한 번만 읽고 고정, 이후 원본 경계 변화는 1회 로그. 책 칸에 가방(Item Rebuild 배지)과 같은 서식의 `x수량` 표시(우측 아래). 훅 25개. 빌드·화면 확인 전. [근거](0.5.10.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.9

상단 스킬 탭 폐지: 버튼 숨김(자리 접기), L2/R2·클릭·K 진입 차단, 스킬 시트 요청은 인물 시트로. 스킬 포인트 `!`는 인물 탭 버튼에 표시. 새 훅 5개(총 25). 스킬 시트 객체는 유지. 빌드·화면 확인 전. [근거](0.5.9.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.8

0.5.7 설치·로드는 해시와 로그로 확인, 사용자 보고로는 Space 무반응(원인 미확정). 물리 Space 입력을 직접 확인하고, 기존 Action_A 경로와 합쳐 한 번 누름에 1회만 학습. 학습/무시 사유를 로그로 남김. 텍스트 입력 중 무시. 훅 20개. 빌드·화면 확인 전. [근거](0.5.8.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.7

학습 패널에서 배울 수 있는 책이 선택돼 있으면 확인 키(Space, `Action_A`)로 1회 학습. 학습 불가면 입력을 원래 경로로 통과. F 변경 없음, 훅 20개. 0.5.6 사용자 빌드 PASS(11,509+3,205, SHA256 8FF6A59D…). 0.5.7 빌드·화면 확인 전. [근거](0.5.7.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.6

원본 스킬 정보창을 더 이상 옮기지 않는다(읽기 전용). 0.5.5 로그에서 열기/닫기마다 정보창 폭 593→5222, 배율 1.0→0.44 누적 확인 → 위젯 차용 방식 제거, 학습 패널만 옆에 배치. 학습 버튼은 원본 모양·원래 크기로 복귀하고 누르는 동안 95% 축소 반응 추가. 배경 alpha 0.8 유지. 훅 20개. 빌드는 사용자 build.ps1 실행 전, 화면 확인 전. [원인·변경·한계](0.5.6.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.5

학습 패널 배경만 alpha 0.8(글자·아이콘·버튼 불투명, 가운데 원본 정보창 불변). 넘치던 원본 `스킬 배우기` 버튼을 숨기고 440×64 모드 버튼으로 교체, 호버·누름·이탈 취소·다음 프레임 1회 학습. 훅 20개 유지. 사용자 PC 빌드 성공, 13,839 검사·20개 훅/311개 참조 PASS, 루트 DLL SHA256 1D82CECC…DD51688F. Mods 설치·화면 확인 전. [변경·검증](0.5.5.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.4

공용 FeatureUI 승격 제거. 연결되어 재생 중인 원본 안내 content만 임시 최상단 이동하고 부모/정렬/입력을 복원한다. 원본 2배 속도 유지. 빌드 경고/오류 0, 13,823 검사·20개 훅/309개 참조 통과. 실제 화면은 사용자 확인 전. [원인·수정·근거](0.5.4.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.3 진단본

짝수 번째 포인트 사용 시 공통 UI 소실을 조사하는 읽기 전용 상태 기록 추가. 기존 동작 유지, 원인/해결 미확정. 포인트당 15초/180개 스냅샷 상한. 빌드 경고/오류 0, 13,799 검사, 20개 훅/305개 참조 통과. [영상·로그·분석·검증](0.5.3.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.2

실행 로그의 반복 종료 두 건은 바깥 좌클릭 경로였다. 입력 이벤트 좌표로 밖에서 누르고 밖에서 뗄 때만 닫도록 보강했다. 좌표/배율/알림 상태 및 Esc/우클릭 구분 로그 추가. 알림과의 간접 관계 및 실전 해결 여부는 미확정. 빌드 경고/오류 0, 오프라인 13,793개 검사, 훅 20개/참조 301개 통과. [근거](0.5.2.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.1

포인트 사용을 포인터 해제 다음 프레임으로 미루고 후속 클릭을 취소한다. 내용 축소 시 패널 위치를 유지하며 닫힘 사유 로그 추가. 간헐적 닫힘의 실제 원인은 로그 없이 미확정이다. 새 포인트 획득 연출은 다음 레벨업 입력에서 원본 완료 콜백으로 마무리하고 원래 입력을 진행한다. 능력치/경험치 정산 대기는 유지한다.

Release 경고/오류 0, 오프라인 13,791개, 20개 훅/299개 참조 검증 통과. 게임·Steam 실행/자동 설치 없음. 실제 증상 해소는 사용자 확인 전. [분석·근거](0.5.1.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.5.0

창 바깥 좌클릭으로 정보/학습 창을 닫고 해당 클릭의 배경 실행을 취소한다. 우클릭/Esc 유지. 스킬업 버튼은 `스킬 레벨 +1`, 파란색 호버/누름 효과와 버튼 내부 release 적용·밖으로 벗어나면 취소를 추가했다. 자체 알림을 제거하고 원본 알림의 재생 속도만 학습 범위에서 2배로 조정하며 종료 시 복원한다.

Release 경고/오류 0, 오프라인 13,782개, 15개 훅/291개 참조 검증 통과. 루트 DLL 제공. 게임·Steam 실행/자동 설치 없음. 실제 화면/조작은 사용자 확인 전. [버전·근거](0.5.0.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.4.1

화기 클릭 시 0.4.0에서 보고된 LocalToGlobal NullReferenceException을 수정했다. displayObject 없는 그룹은 배치 이동 대상에서 제외하고 실제 표시 자식은 유지한다. 비표시 요소의 좌표는 부모 기준으로 처리한다. 인접 패널/스타일/책 3행/포인트 기능 유지.

Release 경고/오류 0, 오프라인 13,772개, 15개 훅/268개 참조 검사 통과. 루트 DLL 제공. 게임·Steam 실행/자동 설치 없음. 실제 화면은 사용자 확인 전. [원인·재현·검증](0.4.1.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.4.0

중앙 팝업을 제거하고 원본 스킬 정보창 옆에 책·스킬 포인트 학습 패널을 배치한다. 원본 정보가 오른쪽이면 추가 패널도 그 오른쪽, 반대면 왼쪽이다. 원본 위젯/배경/글자색을 재사용하며 두 창의 비례 간격·화면 경계·클리핑을 적용한다. 책 목록 최대 3행, 포인트 사용/책 조건/금지 표식/수량/최상단 알림 유지. 암전과 전체 입력 차단 제거, 다른 스킬 클릭으로 전환 가능. 닫기/Esc/우클릭으로 패널 종료.

Release 경고/오류 0, 오프라인 13,769개 검사, 15개 훅/267개 참조 대조 통과. 루트 DLL 제공. 자동 설치·게임·Steam 실행 없음. 실제 0.4.0 화면/입력/학습/병용은 사용자 확인 전. [버전·근거](0.4.0.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.3.0

책 그림 비율 기준 수량 여백·두 자리 수 안전 표시, 양쪽 열 줄바꿈/클리핑·중앙 구분선, Esc/우클릭 닫기(버튼 문구: 닫기), 팝업 위 3초 학습 메시지를 추가했다. 인물 탭 하단에 선택 인물의 남은 스킬 포인트를 표시하고 각 스킬 팝업에서 원본 DB 경로로 1포인트를 사용한다. 책이 없어도 포인트 사용 가능. 언어 팝업은 포인트 사용 제외.

Release 경고/오류 0, 오프라인 12,464개 검사, 14개 훅/243개 API 참조 검증 통과. 루트 DLL 제공. 자동 설치·게임·Steam 실행 없음. 실제 0.3.0 화면/입력/학습/병용은 사용자 확인 전. [버전·근거](0.3.0.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.2.1

0.2.0 사용자 로그의 GGraph 중복 등록 경로를 수정했다. 팝업 갱신에서 원본 스킬창 전체 렌더를 제거하고 자체 미리보기 행으로 책 상세만 갱신한다. 창은 내용 기준 높이의 두 열(빈 목록은 한 열), 책 목록 최대 세 줄, 1배 이상 확대 없음으로 변경했다. 장비 보너스 한 줄 표시.

Release 경고/오류 0, 오프라인 12,429개 검사, 13개 훅/216개 참조 검증 통과. 루트 DLL 제공. 설치·게임·Steam 실행 없음; 0.2.1 실제 화면/학습/병용은 사용자 확인 전. [버전·진단 근거](0.2.1.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.2.0

인물 스킬 행/언어 영역에서 관련 보유 책 팝업을 연다. 원본 스킬북 상세 패널과 금지 판정·조건·경험치·학습을 재사용하고 스킬 설명/레벨/장비 보너스/학습 후 미리보기 및 ×수량을 표시한다. 기존 인물 페이지 유지. 상단 탭은 아직 유지한다.

Release 경고/오류 0, 오프라인 12,419개 검사(페이지 11,509 + 팝업 910), 훅 13개/API 참조 190개 및 시작 그래프 검사 통과. 루트 DLL 제공, 자동 설치·게임·Steam 실행 없음. 화면·실전 학습·병용 확인 전. [버전과 근거](0.2.0.md) · [패치](PATCHES.md).

아래는 이전 버전 기록이다.

# Tab Characters — 0.1.2

10명 중앙 정렬 페이지·클릭 선택 유지. 해당 역할 목록 풀의 비표시 초상화 URL/남은 MyGLoader 작업을 정리하고, 인물 탭에서 메뉴를 닫을 때 큰 일러스트와 상세 초상화를 정리한다. 빈 UI 재사용, 현재 페이지/선택 상세 유지. 원본 전체 명단·3D·공용 리소스·GC 변경 없음. 신규 훅 없음(9개 유지).

Release 경고/오류 0, 관리 대역 11,509개, 훅 9개·게임 API 참조 60개 대조 통과. 게임/Steam 실행 없음. 참조 정리의 실전 안정성·메모리 절감량은 사용자 확인 전.
사용자 0.1.0 표시 보고는 이전 기록에 보존하며 0.1.2 실행 결과로 취급하지 않는다.

[사용법](../../../TabCharacters/README.md) · [패치](PATCHES.md) · [버전 근거](0.1.2.md) · [이전 0.1.1](0.1.1.md) · [설치 기록](../../../TabCharacters/evidence/deployment.json)
