# Restitutor.Core 0.1.0 (2026-09-22)

설치 위치: `Sailing Era\UserLibs\Restitutor.Core.dll` (MelonMod 아님, 공통 라이브러리). SHA256 `928E5F54948C12F769085780447F7E4647B14C86BA076EA0ABEEA7AC55E28027`.
소스: [Core/src](../../../Core/src) · 계획: handoff/CORE_PLAN.md

## 들어 있는 것 (이것만)
| 공개 API | 하는 일 | 대체하는 기존 코드 |
|---|---|---|
| `CoreInfo.Version` / `Require(log, "x.y.z")` | Core 버전 확인. 미달이면 로그 1줄, 그 모드만 비활성 | 없음(신규) |
| `HookSet(harmony, handlerType).Hook(target, name, prefix, postfix, finalizer, args, declaredOnly=true)` | 대상 정확히 1개 확인, 처리기 전부 먼저 확인 후 패치. 실패 시 아무것도 패치 안 함 | 각 모드의 `Hook`/`Patch`/`Required`+`H` 도우미 |
| `HookSet.InstallAll(log, what, body)` | 설치 전부 성공 또는 그 모드 훅 전부 제거 + 오류 1줄 | 각 모드 `try { … } catch { UnpatchSelf(); }` |
| `GameReady.Run(owner, action)` / `IsReady` | 첫 장면 진입 프레임(`SceneManager.IsSceneEntered`)에 1회 실행. 이미 준비됐으면 즉시. 처리기마다 예외 격리 | Contribution·Item Rebuild·HookCensus 의 지연 등록 |
| UIManager 조기 훅 거부 | 준비 전에 `Il2CppClient.Manager.UIManager` 훅 시도 → 예외(패치 안 함) | 검은 화면 사고 방지 규칙(item-rebuild 0.1.1) |

기능 로직·입력 공통 처리 없음(입력은 5단계).

## 비용
- 모드가 Core 를 처음 쓰는 순간 프레임 콜백 1개 등록 → 장면 진입이 확인되면 그 프레임에 제거. 그 뒤 매 프레임 비용 0.
- 훅은 각 모드 자기 Harmony 인스턴스에 등록(모드를 빼면 그 모드 훅만 사라짐). Core 자신은 훅이 없음.

## 버전 규칙
- AssemblyVersion 은 1.0.0.0 고정(새 Core 가 옛 모드를 항상 만족). 실제 버전은 `CoreInfo.Version`(속성이라 실행 시 읽음).
- API 는 추가만. 바꾸거나 지우지 않음.

## 검증
- Release 빌드 경고/오류 0 (SDK 10.0.401, net6.0, 실제 MelonLoader/Harmony/Assembly-CSharp 참조).
- 관리 코드 검사 48개 통과(`Core/tests`): 버전 비교, 대상 찾기(오버로드·by-ref·상속·private/static), 처리기 누락·중복, 누락 시 패치 0, InstallAll 실패 시 전부 제거, UIManager 준비 전 거부·준비 후 허용, 준비 전 대기·순서·예외 격리·중첩·1회만·준비 후 즉시·프레임 콜백 제거.
- 게임 확인(2026-09-22 02:22 로그, 사용자 간단 테스트 "스무스"): UserLibs 에서 로드(`Melon Assembly loaded: '.\UserLibs\Restitutor.Core.dll'`; MelonMod 가 아니라 `0 UserLibs loaded` 로 집계됨), `Restitutor.Core 0.1.0 loaded.`, Instant Entrance 0.1.1 등록 성공, 오류 없음.
- 준비 시점: 02:22:40.815 — 모드 로드 3초 뒤, 시작 화면 안내문 건너뛰기(02:22:40.855)보다 먼저. 즉 **세이브 로드 전 시작 단계**에서 켜짐(HookCensus 등록과 같은 프레임).
- 확인 안 됨: Core 파일이 없을 때 모드가 오류 1줄로 비활성되는지.
- 확인됨(로그): MelonLoader 가 시작 시 `Loading UserLibs...` 를 수행(2026-09-22 01:38 로그, 당시 0개).
