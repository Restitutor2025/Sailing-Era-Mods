# Restitutor.Core 0.2.0 (2026-09-22)

CORE_PLAN 5단계(입력 공통 처리) 묶음 3. **기능 변경 없음**(입력 판정 로직 동일).
설치: `UserLibs\Restitutor.Core.dll`, SHA256 `36B96E0839E969CB36607A11A631917DDFCE3B6B08F756948FFA8DFEAE0EFA5E`. 이전: 0.1.0 (928E5F54…).

## 바뀐 것
- 추가(기존 API 변경 없음): `InputGate` — `InputSystemManager.OnEventCaptureInput` 에 Core 전용 Harmony(`restitutor.core.input`) Prefix 1개(첫 등록 때 설치). 모드는 `HookSet.Input(owner, ctx => bool)` 로 처리기 등록. 모든 처리기를 등록 순서대로 실행(단락 없음), 하나라도 false → 입력 막음, 예외 → 모드 이름과 함께 1회 기록 후 허용. 목록은 copy-on-write(실행 중 제거 안전).
- 추가: `HookSet.RemoveAll()` — 그 모드의 Harmony 훅 + 입력 처리기 제거. `InstallAll` 실패 시에도 RemoveAll 사용(입력 처리기까지 제거).
- 0.1.0 으로 빌드된 모드(Instant Entrance, Stat Rank, Devil Fruits, Intro, Text Speed, CTRL Instant, Item Rebuild)는 그대로 동작(AssemblyVersion 1.0.0.0 고정, 요구 버전 0.1.0 ≤ 0.2.0). 규칙대로 전 모드 0.2.0 으로 재빌드·전 검사 통과 확인, 재배포는 안 함.

## 검증
Release 빌드 경고/오류 0. Core 검사 60 통과(입력 게이트 13 추가). 전 모드 재빌드 경고/오류 0, 전 검사 통과(CTRL 13, Contribution 54+196, Core 60, Devil Fruits 10, Instant Entrance 15, Item Rebuild 5676, Map 22+38, Stat Rank 9, Tab Characters 3224+11511, Text Speed 48). 게임 확인 전.
