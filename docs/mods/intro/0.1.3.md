# Intro Skip (Restitutor fixes) 0.1.3 (2026-09-22)

Restitutor.Core 로 전환(CORE_PLAN 4단계 묶음 1). **기능 변경 없음.**
설치: `Mods\Additional_Functions\Restitutor_Additional_IntroSkip.dll`, SHA256 `7C4D7F5C6470236A808BFA83063E3F7818166AECA87F163EE5CF124CFB7F5CB6`. 필요: `UserLibs\Restitutor.Core.dll` 0.1.0 이상.
이전 소스 `archive-intro/0.1.2/IntroSkipMod.cs`, 이전 DLL `releases/intro/0.1.2/` (F86B494A…, 설치본과 같음).

## 바뀐 것
- 패치 4개(UILaunchView.OnInit·HideHook Postfix, UpdateHook Prefix, Transition._Play Postfix) 등록을 `HookSet.Hook(..., declaredOnly: false)` 로. 기존 찾기(Instance, 상속 포함, 이름 1개)와 같은 메서드: 메타데이터에서 static 포함해도 각 1개(hookcheck).
- 실패 시 disabled=true + 전체 해제 동일. StopSplash() 는 Core 와 무관하게 먼저 실행(기존 순서).
- Core 버전 확인, Core 파일 없으면 오류 1줄 후 이 모드만 비활성(원래 시작 화면 유지).

## 검증
Release 빌드 경고/오류 0. 검사 프로젝트 없음(기존에도 없음). 등록 대상 메타데이터 확인 4/4. 게임 확인 전.
