# HookCensus 0.1.0 — 훅 대상 호출 빈도 계측(진단 전용)

2026-09-22. 목적: RESOURCE_AUDIT B절(상시 훅이 걸린 네이티브 함수)의 실제 호출 빈도 측정. 19개 대상에 카운터 Prefix(정수 증가만). 5초마다 `[CENSUS] … scene=harbor|ocean|loading|other: 대상=초당 호출` 1줄, 종료 시 장면별 평균 `[CENSUS-TOTAL]`. 첫 장면 진입 후 등록(UIManager 조기 패치 금지).
대상: Transition._Play, BaseObjectData.GetPointProperty/GetProperty, BoatEntityOceanDriver.FixedUpdate, Animator.Play(string,int,float), GList.set_numItems/ScrollToView/HandleArrowKey/Dispose, FunctionOpenDB.GetFunctionData(int), PlayerBagDB.GetFreeCapacity, UIMapHarbourIcon.UpdateInfo, UIBase.get_IsInputActive, InputSystemManager.OnEventCaptureInput, UIManager.SetFocusOnUIView, UIPropStoreModel.get_SellCount, UIMapView.Refresh, UIHarborCtrl.ShowMainUI, UIHeroLevelUpModel.get_LevelGetSkill.
설치 `Mods/Analytics/Restitutor_Analytics_HookCensus.dll`. 한 번 플레이 후 제거. 소스 `HookCensus/`.
