# Cheats Interface 1.6.4 — 키 안내를 제목 칸 안 작은 글씨로 (2026-09-24)

상태: **구현됨-미확인**(VM 빌드 경고/오류 0, lifecycle 166 통과 = 1.6.3 의 162 + 신규 4, 게임 확인 전).
설치: `Mods/Cheats/Restitutor_Cheats_Interface.dll` SHA256 `9912C96A657F1A666FFA21D90CFF03A2B0FF29964297D6FF0BEF6E2E8923885A` (사본 `Cheats/releases/1.6.4/`). 되돌리기: `Cheats/releases/1.6.3/` DLL 덮어쓰기.

## 배경
1.6.3 게임 확인(사용자 스크린샷, 2026-09-24): 안내 줄이 제목 칸 테두리 밖 아래에 크기 14 로 나옴. 동작(접으면 숨김)은 정상. 사용자 요청: '통합 치트' 아랫줄에 작은 글씨로.

## 변경 (사용자 결정 2026-09-24: 제목 칸을 늘림)
- 안내 글자 `CheatKeyHint` 를 제목 칸(`CheatTitleDrag`) 안으로 옮김: x 15, y 39, 크기 **10**(14 → 10), 폭 = 제목 칸 폭 − 20(최소 223).
- 펼친 창: 제목 칸 48 → **64**(테두리 44 → 60), 목록·스크롤바 시작 y 80 → **70**, 창 높이 = 목록 + 76(1.6.3 +86).
- 접은 창: 제목 칸 48, 안내 줄 숨김 — 1.6.3 이전과 같은 모양.
- 제목 칸 테두리는 폭 또는 높이가 바뀔 때만 다시 그림(`drawnHeaderH` 추가). 훅·판정 추가 없음. 로그 `Cheats Interface 1.6.4 loaded (key hint in small type inside the title box; …`.
- 크기 10 근거[추정]: 1.6.3 스크린샷에서 크기 14 문구 폭 ≈ 296(논리 px) → 크기당 ≈ 21 px, 제목 칸 안쪽 최소 폭 223 → 10 이면 ≈ 211. 게임 글꼴 실측 아님.

## 게임 테스트
1. 로그 `Cheats Interface 1.6.4 loaded`.
2. 펼친 창: 제목 칸 테두리 안 '통합 치트' 아래 작은 글씨 한 줄, 잘림·겹침 없음, 테두리와 첫 패널 사이 간격 정상.
3. H(또는 ∨)로 접으면 제목 칸이 원래 높이로 줄고 안내 줄 사라짐, 펼치면 다시.
4. 제목 칸 끌기(안내 글자 위 포함)·스크롤·패널 클릭 이전과 같음.
