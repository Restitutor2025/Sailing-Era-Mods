# Cheats Interface 1.6.3 — 제목 아래 키 안내 줄 (2026-09-24)

상태: **구현됨-미확인**(VM 빌드 경고/오류 0, lifecycle 162 통과 = 1.6.2 의 156 + 신규 6, 게임 확인 전).
설치: `Mods/Cheats/Restitutor_Cheats_Interface.dll` SHA256 `FA5BF5C80670B07655B8D12006EFED51F8C43A024EC77AF7DF379B4DBAFFA477` (사본 `Cheats/releases/1.6.3/`). 되돌리기: `Cheats/releases/1.6.2/` DLL 덮어쓰기. 이전 소스 `Cheats/archive/interface-1.6.2-source/`.

## 변경 (사용자 요청·결정 2026-09-24)
- 치트 창 제목 줄 바로 아래 한 줄: **`[H] 접기·펼치기   [O] 모든 치트 끄기·켜기`** (문구 = 사용자 선택).
- H 로 접으면(제목 줄만) 안내 줄도 숨김(사용자 결정). 펼친 창은 안내 줄 높이(26)만큼 패널 목록이 아래로 내려가고 창이 그만큼 길어짐.
- 구현: `Host.BuildChrome` 에 글자 1개(`CheatKeyHint`, 크기 14, 폭 = 창 폭 − 30), 목록·스크롤바 시작 y 54 → 80, 창 높이 = 목록 높이 + 86(이전 +60), 화면에 맞추는 목록 최대 높이도 같은 양만큼 줄임. 매 프레임 추가 작업 = 기존 창 갱신 안에서 글자 `visible`·크기 설정뿐(값이 같으면 FairyGUI 가 무시). 훅·판정 추가 없음. 로그 첫 줄 `Cheats Interface 1.6.3 loaded (key hint line under the title; …`.
- [미확인] 글자 폭: 한 줄에 다 들어가도록 크기 14 로 잡았으나 게임 글꼴로 실제 폭은 확인 못 함 → 잘리면 크기 조정.

## 게임 테스트
1. 로그 `Cheats Interface 1.6.3 loaded`.
2. 창 제목 아래 안내 줄이 한 줄로 다 보임(잘림 없음), 첫 패널과 겹치지 않음.
3. H(또는 ∨ 버튼)로 접으면 안내 줄도 사라지고 펼치면 다시 보임.
4. 스크롤·창 끌기·패널 클릭이 이전과 같음.
