# Map 0.2.1 — 항로 도달 불가 도시 표시 복원

2026-09-19. 수정 전 설치 DLL SHA256 `9290028800505EB9465F0007FEE39E59BEF848E1C4093BE90F3AC86B43C764D1`은 0.2.0 후보 dist와 일치했다. 기존 CURRENT의 '후보 미설치/기존 0.1.5 설치' 설명과 실제 상태가 달라 설치 파일을 기준으로 0.2.0 후보 소스를 수정했다. 이전 문서는 당시 이력으로 보존한다.

## 근거와 변경

[현재 바이너리 본문](../../../analysis/user-reports/2026-09-19-map-contribution/REPORT.md). GameAssembly SHA256 `50D53D17829E3E77B9786EA42D998D1AD258F0653846524069F22E5E442EFFCA`.

- 원본 UIMapLineCtrl.ReachedPort(0x5EACA0): 항로 상태에서는 현재 항구를 허용하고, 나머지는 TotalPortDic의 MeasureCanReach(0x2C)와 NoLineReach(0x2D)를 함께 검사한다. 항로 상태 밖에서는 허용한다.
- 원본 UIMapLineHarbourIcon.UpdateInfo(0x7A2D20): 도달 불가이면 GetRedHarbourIcon(port.mapIcon), ctrlSelfPort(0x230) 상태 2, loaderIcon(0x270) URL을 적용한다. 경유지 imgRedFlag와 별도다.
- SharedMap의 소유한 항로 세션에 같은 판정과 표시를 추가했다. UIMapLineCtrl 싱글턴을 호출하거나 생성하지 않는다. 원래 선택 제한과 네이티브 경로 계산은 유지한다.
- 원래 지도 UpdateInfo 후의 URL/컨트롤러 상태를 보관한다. 자체 경유지 갱신에서는 빨간 상태를 기준 상태로 잘못 저장하지 않는다. 도달 가능으로 바뀌면 보관한 상태로 복원하고, 정상 종료/정리 시에도 복원한다. 폐기된 컴포넌트는 건너뛴다.
- 패치 40개 유지. 입력/안개/선택 깃발 계약은 기존 후보를 따른다. 표시 상태의 저장·복원 항목만 추가했다.

## 검증 / 배포

Release 빌드 경고/오류 0. 지도 입력·수명주기·표시 대역 29개, 항로 데이터 대역 22개 통과. 40개 패치 서명과 핸들러, 외부 참조 197개 대조 통과. 지도 대역 프로젝트에서 기존 BaseIntermediateOutputPath 설정 경고 1개가 발생했으며 테스트는 통과했다.

설치 Mods DLL과 [배포 파일](../../../Map/candidates/tab-only/dist/0.2.1/Restitutor_fixes_map.dll) SHA256 일치: `0BCAC342A9678945E0994CF3EBE617D621B865557FD6BB0DAFCE43004C481157`.
이전 설치본은 Map/candidates/tab-only/dist/0.2.0/installed-before-0.2.1.dll에 보존했다.

게임/Steam 실행·조작 없음. 실제 렌더링/재진입/병용은 사용자 검증 전이다. [실전 확인 절차](../../../analysis/user-reports/2026-09-19-map-contribution/USER-CHECKS.md).
