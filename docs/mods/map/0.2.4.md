# Map 0.2.4 (2026-09-22)

CORE_PLAN 5단계(입력 공통 처리) 묶음 3. **기능 변경 없음**(입력 판정 로직 동일).
설치: `Mods\Bug_Fixes\Restitutor_fixes_map.dll`, SHA256 `C94E91615C300271CFFE10BC3D06081200EDFB32300B42465FB816DBE8F09597`. 이전: 0.2.3 (BAC59302…).

## 바뀐 것
- 훅 등록을 처리기 클래스별 HookSet(같은 Harmony 인스턴스)으로. 찾기 조건(DeclaredOnly, 인자 지정 시 정확히) 그대로.
- 입력: 자체 `InputSystemManager.OnEventCaptureInput` Prefix 를 없애고 Core 공용 입력 게이트(`HookSet.Input`)에 같은 처리기를 등록. 판정 로직(무엇을 막는지)은 그대로. 게이트는 등록된 처리기를 모두 실행하고 하나라도 false 면 입력을 막음(= 여러 Prefix 가 따로 있던 때와 같음). 처리기에서 예외가 나면 1회 기록 후 "허용"으로 처리(이전엔 예외가 원본 호출 쪽으로 전파될 수 있었음). 대상: CaptureClose(항로 계획 창 Start/지도 키로 닫기).
- 설치 실패 시 전체 해제는 `InstallAll`(입력 처리기 포함). 필요 Core 0.2.0.

## 검증
Release 빌드 경고/오류 0. 검사 22+38 통과. 훅 대상 메타데이터 확인(OnAction_A 두 서명, SetLane() 포함). 게임 확인 전.
