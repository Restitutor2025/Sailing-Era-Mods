# Map 0.2.3 — 빨간 항구를 원본 분기로 그림(드래그 깜박임·스터터링 제거)

2026-09-22. 근거: `handoff/FEEDBACK_2026-09-22.md` 4·6절, 0.2.2-diag 로그(드래그 중 초당 최대 3331회 url 되돌림/덮어쓰기).

- 기능 동일: 도달 불가 항구(현재 항구 제외, TotalPortDic 에 없거나 MeasureCanReach·NoLineReach 중 하나라도 false)는 빨간 아이콘 + 컨트롤러 2. 선택 항구 깃발, 경로 강조 그대로.
- 방식: 원본 UIMapHarbourIcon.UpdateInfo 의 빨간 분기(`_eMapUseType==1` + `RedPort` 포함) 사용. 도달 불가 아이콘의 UpdateInfo 호출 동안만 `_eMapUseType=1`(Prefix), Finalizer 에서 원래 값 복원. id 는 RedPort 에 세션당 1회 추가, 세션 종료 시 추가한 것만 제거. 원본이 매 프레임 같은 url·상태를 쓰므로 재로딩 없음.
- 삭제: UpdateInfo 뒤 url·컨트롤러 덮어쓰기, 풀 재사용 포인터 기준 복원 딕셔너리(네이티브 경로에서). 도달 판정 딕셔너리 조회 3회 → 1회.
- 안전장치: 첫 세션에서 필드 오프셋 확인(`_eMapUseType`=0x68, `RedPort`=0x78, `HarbourId`=0x38). 다르면 경고 1줄 후 0.2.1 덮어쓰기 방식 사용.
- `[MAPDIAG]`: 1초당 Refresh·UpdateInfo·nativeRedCalls·RefreshRoute·IsInputActive(관리 코드 호출 수) 정수 집계만(세션 중). 출항 준비 수명 줄 유지.
- UIBase.get_IsInputActive: 네이티브 직접 호출자 0개(56,444 함수 검사, 인라인) → 훅 유지(기능 보존), 호출 수만 계측.
검증: 빌드 0/0, SharedMapChecks 38(네이티브 경로 9개 추가), RouteDataChecks 22. 게임 확인 전.
DLL `Map/candidates/tab-only/dist/0.2.3/Restitutor_fixes_map.dll` SHA256 BAC59302779B02B725BC2E5B9D48FE2D146F523A4CA2526627F7BEB0D43BC9EC.
