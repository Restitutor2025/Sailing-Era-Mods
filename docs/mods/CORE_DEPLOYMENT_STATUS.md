# 2026-09-24 CityEditor Core 다운그레이드 확인

## 현재 상태

**미복구. 게임 설치본과 에디터 배포 원본이 모두 구버전 0.5.0이다.** 사용자 정상 적용 보고가 있는 0.7.7 패키지는 별도로 존재한다. 이 문서 변경은 DLL을 설치하거나 배포 코드를 수정하지 않는다.

## 확인된 근거

- 사용자: 셀레네 선택 클릭 무반응, 셀레네/로마 제국 상세 표시 소실, 포커스 위치 이상.
- 2026-09-24 14:41:27.948 도시 에디터 `cities_deployed` 이벤트: Core DLL을 게임 Mods에 복사, errors=[] (events-20260924-143943-43276.jsonl, seq117).
- 15:34:01 게임 로그: `Restitutor CityEditor Core v0.5.0` 로드.
- 15:34:44 이후: `추가 156 클릭 — 안내만` 반복. 시작 무반응을 직접 설명하는 실행 근거다.
- 게임 `Mods/Restitutor_CityEditor_Core.dll`과 에디터 `core/release/Restitutor_CityEditor_Core.dll` SHA256이 일치: `07565b30d6072f0a783b5b920f9b52e6f49f9eccb4bb48e74b4ce7a54f1dbf37`.
- 사용자 정상 적용 보고가 있는 0.7.7 보관 패키지의 SHA256: `5a205a288287ea70f410ac4cb5da149e0ebb28c07f362ab3b607989f1ec4a312`.
- 에디터 `editor/se_editor/export/citysave/deploy.py`는 `settings.core_dll`의 해시가 설치본과 다르면 버전 비교 없이 `shutil.copy2`한다. 원본 경로는 `core/release`다.

## 판단과 한계

도시 에디터 적용 경로가 구버전 DLL을 다시 설치했다는 근거가 확보됐다. 특정 AI나 사람이 직접 덮어썼다고 식별할 근거는 없다. copy2는 원본 수정 시각을 보존하므로 DLL LastWriteTime은 실제 설치 시각이 아니다. 포커스 편차는 별도로 측정하지 않았다.

## 복구 전 주의

기존 0.7.7 설치기는 0.7.6 선행 해시 또는 0.7.7 해시만 허용하므로 현재 0.5.0을 거부한다. 검사 우회나 구형 설치기 재실행을 안내하지 않는다. 현재 파일을 백업하고 확인된 해시를 허용하는 복구 절차가 필요하다. 게임 설치본만 복구하면 다음 도시 적용 때 같은 문제가 반복될 수 있으므로 에디터 배포 원본·소스 기준을 함께 정리해야 한다.

추가 작업: 원본 소스 커밋과 0.7.7 패키지 연결 확인, 두 배포 위치 정합성 복구, 다운그레이드 자동 차단, 오프라인 검사, 사용자 재시험. 아직 수행하지 않았다.

진단 중 게임/Steam 무조작. 전체 로그·DLL·게임 추출물은 커밋하지 않으며 로컬 근거는 모드 작업 폴더 `analysis/hero-stage2/rollback-20260924/`에 보존했다.
