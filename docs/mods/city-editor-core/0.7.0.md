# 0.7.0 — 시작 설정과 항구 초기화 진단

2026-09-23, Codex. 게임/Steam 실행 없음. 기존0.6.0 사용자 실행에서 장주252 도착·IsBornCompleted=true 확인. 항구시설 초기화 NullReferenceException의 대상 항구/원인은 아직 확정하지 않았다.

## 변경
- Core: 새 게임 요청~첫 입항 동안 TemplateUtils.GetPort(int), GetPortFacilityLineByType(int,int), TemplateManager.GetWharf(int), WorldPortData.InitFacilityData() Postfix 관측. 첫3개는 조회 인수와 존재 여부, 마지막은 정상 반환 기록. 생성/도착 시 FunctionData와 OpenFacilityList 스냅샷. 최대180초/조회2048항목, 중복 및 예외 제한, 원본 인수/결과 불변. 진단 후크 설치 실패는 개별 기록하며 새 게임 경로를 차단하지 않는다.
- hero-start.json으로 진단 여부와 첫 입항 이벤트 설정. 이벤트0이면 호출 없음. 기존 일반 GameEvent만 지원하며 원본 CheckAndTriggerGameEvent 조건/확률/완료 판정을 사용해 한 번 요청. PendingTid를 먼저 해제해 재진입 중복 방지. 실제 이벤트 재생 미검증.
- 에디터: 시작 설정 탭, 이름/항구 검색/해금 체크/일반 이벤트/진단, 저장·undo, 시작 필드만 설치 데이터에 병합, 검토 후 바뀐 파일 거부·백업·부분 실패 복원. 통합 진입에서 선택 화면 적재 누락 수정.
- 첫 시험값: Selene156 이름 셀레네 / Port253 케페 / Facility0 / 해금[25] / 이벤트0 / 진단true. 사용자 승인. 원본253에는 시설7(학원/서고)이 없어 잠김 자체는 실패 증거가 아니다. 능력치·자금·모델·이미지·선택 좌표 보존.

## 근거
설치 GameAssembly SHA256 50d53d17829e3e77b9786ea42d998d1ad258f0653846524069f22e5e442effca.
대상 RVA: GetPort 8AF600, GetPortFacilityLineByType 8AC490, GetWharf 3FC640, InitFacilityData B763D0. 메타데이터에서 각 주소별 다른 대상 별칭 없음. 본문에서 항구/시설/부두 조회의 NullReference 가능 경로 확인. 특정 도시를 원인으로 단정하지 않는다.
CheckAndTriggerGameEvent(int,Type) 4D16A0→TemplateUtils.GetGameEvent 8ADAF0→일반 GetGameEvent 3BF760; 확률·조건·반복/완료 기록 확인. 반환bool은 요청 결과이며 화면 재생 성공을 보장하지 않는다.

## 검증
Release 경고0/오류0. 오프라인 시작 설정23검사(설치 필드 보존, undo/save/load, 실패 주입 복원, 오래된 적용 계획 거부, 실제 Qt 위젯/통합 로더 경로) 통과. 기존 table.bin 핵심검사133/133 통과(선택 원본/대화 그림은 이 모드에서 생략). 실제 게임의 새 후크 설치, 예외 대상, UI·시설·이벤트·원본5명/기존 저장 회귀는 사용자 확인 전.

배포: [안내](../../../output/hero-start-0.7.0/package/README.md) · [검사 기록](../../../output/hero-start-0.7.0/tests-start.txt) · [새 진단 설계](../../../analysis/hero-stage2/DIAGNOSTIC-0.7.0.md).
