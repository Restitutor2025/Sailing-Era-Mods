## 2026-09-24 사용자 실전 성공 보고

사용자 원문: “테스트완료 케페에서 출항해서 바르나까지 갔었어”. Core0.7.4 패키지 재실행 안내 이후 보고로, 케페 출항 및 바르나까지 이동 성공을 사용자 실행 근거로 기록한다. 출항 차단은 해당 시험에서 해소됐다. 대화 종료의 내부 상태 전환은 이번 보고에 로그가 첨부되지 않아 추가 검증하지 않았다. 선택 화면 좌우 전환·스포트라이트는 별도 미완료 사항으로 유지한다. 게임/Steam은 에이전트가 실행하지 않았다.

# Core 0.7.4 — 셀레네 시험 시작 대화와 출항 차단 보완

2026-09-24. 사용자가 별도 시험 시작 대화 연결을 승인했다. 원본 대화 재생·종료 경로를 사용하는 수정이며 게임/Steam 미실행, 실전 성공 여부는 아직 미확인.

## 확정 근거와 이전 설명 정정

0.7.3 사용자 실행 00:05:02.026 이후 CheckQuickEnterOcean 전후 기록: 셀레네156/케페253, 기능20·21 열림, ready=True, ban=False, talkPlaying=False, talkWaiting=False, CurrentTalkPartId=11001. 출항 함수 RVA963CD0의963E8E~963E91은 현재 대화파트>0이면 무안내 반환한다. 선원·물자 검사와 이벤트/임무 제한 검사보다 앞이다. 배 생성110 성공과 시작완료True도 로그로 확인했다.

PlayerTalkDB.InitHook RVA6821C0는0x68224D에서 현재 대화파트에11001(0x2AF9)을 넣는다. 설치 원본에는 TalkPart11001이 없다. 그러나 GameEvent11001은 존재하며 하이르 오우리 합류/역병 이벤트다: GameEvent11001 → GameActionTpl1100101 → TalkPart1100101 → 후속 대화 → Role110 합류. 이벤트ID와 대화파트ID는 별도 영역이다. 앞서 이벤트도 없다고 설명하거나11001을 셀레네용으로 등록하자고 한 것은 오류다. 해당 이벤트·행동·조건·대화·보상은 모두 보존한다.

## 원본 정상 흐름에서 누락된 처리

LoadArchiveCommand.EnterGame RVA827940는 BeforeBornStory가['0']이면 항구로 직접 간다. 원래 도입부는 PureStoryScene.OnEnter 콜백 RVA10C2B70에서 StoryManager.ShowTalkPart RVA9D3C40를 호출한다. ShowTalkPart → CheckPlayTalkPartState RVA9D3AF0는 실제 파트가 존재하면 CurrentTalkPartId를 그 파트로 갱신하고 dirty 처리한다. EndTalkPart RVA9D6180 → SetTalkPartEnd RVA9D2D90는 완료된 실제 파트를 기록하고 CurrentTalkPartId를0으로 정리한다.

셀레네의 스토리 없는 시작은 그 재생/종료 과정을 모두 건너뛰었다. 최종 수정은 직접0 대입이나 허위 완료 기록 대신 승인된 짧은 시험 대화를 원본으로 재생한다. PureStoryScene의 고정 도입 연출은 사용하지 않고, 기존 항구 도착 완료 경로 뒤 항구가 실제 준비됐을 때 같은 ShowTalkPart API를 사용한다.

## 변경

- hero-start.json 새 선택항목 startTalk=15607401. startEvent=0 유지; 두 옵션 동시 양수 거부.
- 전용 TalkPart15607401 → Talk156074001. 한국어 문구: '케페에서 새로운 항해를 시작하자.' 설명형 한 줄(Type2), 후속파트/보상/행동 없음. 원본 템플릿 복사 후 전용 ID로 등록하며 충돌 시 새 게임 전 검증 실패. 영/중번/일 문구도 전용 TalkTextLib 키156074001로 추가.
- 기존 EnterHarbourAniEnd Postfix가 현재 새 게임의 실제 주인공·항구·시작완료 일치 시 대화를 예약. OnUpdate에서 최대30초 동안 항구 UI visible/IsBan=false/ready=true, 대화 재생·대기 없음, 현재파트0 또는 원본초기값11001일 때1회 ShowTalkPart 요청. 다른 파트가 진행 중이면 덮어쓰지 않는다. 타임아웃/주인공·항구 변경/예외에는 예약 해제.
- native end callback은 현재파트/재생/대기 상태만 로그로 확인. CurrentTalkPartId 직접 대입, SetEndTalkPart 강제호출, GameEvent11001 강제실행, 출항 검사 우회 없음.
- 에디터 '시작 설정 · 진단'에 없음/셀레네 시험 대화 선택 추가. 문서 저장·설치값 가져오기·시작 설정 내보내기에서 옵션 보존, 대화 연결 존재/동시이벤트 검사.
- 기존 슬루프110·5000·케페253·기능1~10 및 기존시설·이미지·위치 유지. 저장파일 직접 편집/기존 저장 자동보정 없음. 새 게임에서 시험한다.

## 접점과 제한

기존 출항관측/선택 후크 유지. 새 데이터용 InitTalkPart/InitTalk/InitTalkTextLib(4언어) 후크6개가 기존 HeroRows 삽입기에서 추가된다. 모두 Postfix이며 기존키 덮어쓰기 금지. ShowTalkPart가 실제로 대화 상태/일시정지/항구 UI를 제어하고 종료 때 복원한다. 텍스트속도/대화/항구 UI 모드와 같은 흐름이며 병용 실전 검증 전이다. 소유 콜백은 종료 시 참조 해제하고 새 게임 예약은 시작 시 리셋한다. diagnostic 옵션과 달리 시작 대화는 게임플레이 설정이므로 진단OFF여도 실행한다.

## 설치와 확인

package/APPLY_HERO_DEPARTURE.bat: 기존 주 작업폴더 대신 codex/hero-departure-fix 격리 작업트리. 캐시 origin/main3f5ddd8 기반, 해시검사·백업·원자교체·실패롤백 유지. 게임을 종료하고 설치한 뒤 셀레네 새 게임 → 케페 시험대화 확인/종료 → 출항 시험. 로그 [HeroOpening] scheduled / ShowTalkPart accepted=True current=11001->15607401 / native end callback current=0 / [HeroDeparture] EnterOcean 도달 여부로 단계 확인.

근거: analysis/hero-stage2/departure-20260924/user-core073.log, FINDINGS.md, analysis/hero-stage2/native의 위 RVA 본문. GameAssembly SHA256 50d53d17829e3e77b9786ea42d998d1ad258f0653846524069f22e5e442effca, table bundle d64de77487b5625d75292773c968566bc5d6334372bf9af87906796279787e66.


## 2026-09-24 설치 입력 재검증
사용자 BAT 실행은 게임 파일 변경 검사에서 중단됐다. 현재 게임 DLL은 FileVersion0.5.0.0, SHA256 07565b30d6072f0a783b5b920f9b52e6f49f9eccb4bb48e74b4ce7a54f1dbf37이며 기존 에디터 core/release DLL과 바이트 일치한다. 그 release 파일은 git 변경 없음. 에디터 citysave 적용 경로는 이 release를 복사한다. 실제 마지막 덮어쓰기 주체는 미확인이다. 이전 패키지 입력 해시 25130fcfa141e55df048b58419834c6830061192e5e908f5aa48bdef48703cd8에서 확인된 0.5.0 해시로 DLL 한 항목만 갱신했다. 출력 0.7.4 DLL·소스·게임 데이터는 불변. 보호 검사/백업/원자교체 유지. 기존 에디터의 재적용은 다시 구 DLL을 복사할 수 있다. 게임 실행 없이 CheckOnly로 재검증한다.
