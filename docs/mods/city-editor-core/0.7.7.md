# Core 0.7.7 — 기존 이름 이미지와 원본 구도

2026-09-24. 0.7.6 사용자 영상에서 이름이 뒤집히는 것을 확인. 원본 compNameChinese.texName 초기 rotation=180, compNameEnglish.texPlace rotation=0을 패키지 해독으로 구별했다. 0.7.6에서 원본 이름 컴포넌트의 애니메이션 전 초기값을 그대로 사용한 결함이다.

설치된 name_01은 장식 배경의 셀레네/로마, name_02는 셀레네, name_03은 Selene이다. 사용자는 이미지 제작 오류를 인정하고 기존 이미지로 원본과 같은 색상·구도를 요청했다.

name_02 PNG를 기존 LoadCreationImage로 로드하여 원본 소속 텍스트의 #E4FFFF 색상으로 tint한다. 이미지 종횡비 유지, 이름/가로 구분선/로마 제국 순서로 중앙 정렬. 소속 PNG가 없으므로 소속은 원본 텍스트 스타일로 설정값을 표시한다. 원본 이미지의 음영·서체는 보존하므로 그림의 모든 픽셀이 단색으로 바뀌는 것은 아니다. 파일 자체는 변경하지 않는다. 이미지가 없는 다른 추가 주인공은 원본 이름 텍스트에 rotation=0을 명시하는 대체 경로를 사용한다.

추가 후크·입력 변경 없음. 이름 이미지의 NTexture는 기존 모달 소유 목록에 들어가 root 해제 후 Dispose된다. 기존 취소·키 아이콘·등장 시점 수정 포함. 시작 데이터/게임/Steam 무조작. 다른 모드 병용·시각 결과는 사용자 게임 확인 전.

격리 브랜치 fix/hero-name-image. Core Release 빌드 경고0/오류0. 배포 output/hero-name-0.7.7/package/APPLY_HERO_NAME.bat: 현재 설치 해시 검사, 백업, 원자 교체. main 및 GitHub 변경 없음. 설치 후 이름 방향/색·소속 및 구분선 배치/재진입을 확인해야 한다.

## 2026-09-24 사용자 적용 확인
사용자 보고: ‘완료 제대로 적용됐어’. 0.7.7 적용 안내 직후 이름 이미지·색상·구도 수정의 정상 적용을 확인한 것으로 기록한다. 추가 영상/로그는 없으므로 재진입·창 크기 변경·다른 모드 병용 전체를 검증한 것으로 확대하지 않는다. 게임/Steam은 에이전트가 실행하지 않았다.


## 2026-09-24 15:46 KST 복구 완료 — 파일 검증, 실전 재확인 전

사용자 요청으로 게임 Mods와 에디터 core/release의 Restitutor_CityEditor_Core.dll을 검증된 0.7.7 패키지로 복구했다. 두 대상 모두 FileVersion 0.7.7.0, SHA256 `5a205a288287ea70f410ac4cb5da149e0ebb28c07f362ab3b607989f1ec4a312`로 설치 후 재검증했다. 게임 원본 해시와 변경 전 0.5.0 해시를 검사하고 각 DLL을 백업한 뒤 원자 교체했다. 게임·Steam은 실행/조작하지 않았다.

로컬 복구 근거: 모드 작업 폴더 `output/core-0.7.7-recovery/verified-installation.json`, `restore.ps1`, `backups/20260924-154654-b591dd1b72754bd29341b933a657e887/manifest.json`. 소스 패키지: `output/hero-name-0.7.7/package/Restitutor_CityEditor_Core.dll`.

**남은 불일치:** 실행용 에디터 저장소 core 소스는 0.7.4다. 이번 작업은 알려진 정상 패키지를 복구한 것이며 소스 통합/재빌드를 수행하지 않았다. 현재 도시 적용은 같은 0.7.7 DLL을 사용하지만, 기존 소스를 재빌드하거나 별도 설치기를 실행하면 다시 낮은 버전이 설치될 수 있다. 0.7.7 소스 통합과 자동 다운그레이드 차단은 미완료다. 사용자 게임 재시험도 미확인이다.

