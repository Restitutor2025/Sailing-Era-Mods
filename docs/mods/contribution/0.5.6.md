# Contribution 0.5.6 (2026-09-22)

CORE_PLAN 5단계(입력 공통 처리) 묶음 3. **기능 변경 없음**(입력 판정 로직 동일).
설치: `Mods\Additional_Functions\Restitutor_Additional_Contribution_Goods_Recognition.dll`, SHA256 `0412DEEC4F2A8B18AC160B6962D56B3510ACC3B20416300062692F0602AAA45C`. 이전: 0.5.5 (206D8279…).

## 바뀐 것
- 훅 등록을 `HookSet.Hook(..., declaredOnly: false)` 로(기존 상속 포함 찾기 유지). 첫 도시 프레임의 HUD 이벤트 훅 6개도 같은 HookSet.
- 입력: 자체 `InputSystemManager.OnEventCaptureInput` Prefix 를 없애고 Core 공용 입력 게이트(`HookSet.Input`)에 같은 처리기를 등록. 판정 로직(무엇을 막는지)은 그대로. 게이트는 등록된 처리기를 모두 실행하고 하나라도 false 면 입력을 막음(= 여러 Prefix 가 따로 있던 때와 같음). 처리기에서 예외가 나면 1회 기록 후 "허용"으로 처리(이전엔 예외가 원본 호출 쪽으로 전파될 수 있었음).
- OnDeinitialize 는 `RemoveAll`. 필요 Core 0.2.0.

## 검증
Release 빌드 경고/오류 0. 검사 54+196 통과. 훅 대상 메타데이터 확인(UpdateInfluence 두 서명 포함). 게임 확인 전.
