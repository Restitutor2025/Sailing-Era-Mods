# Contribution 0.5.5 — 도시 혜택 HUD 를 이벤트로 판정(기능 동일)

2026-09-22. 사용자 요청: 도시 입장·다른 시설에서 돌아올 때를 트리거로 한 번만 확인. 근거: RESOURCE_AUDIT A3.
- 이전: 도시에 있는 동안 매 프레임 `HarborHud.IsMainScreen()`(포커스 조회 + 조상 노드 순회), 0.1초마다 혜택 문구 재조립, 매 프레임 대기 목록 복사.
- 이후: 판정 트리거 = UIManager.SetFocusOnUIView/SetFocusOutUIView, UIHarborCtrl.ShowMainUI/HideMainUI/ShowHook/CloseHook, 도시 진입·이탈, 체류 도시 변경, 알림(Journal.Revision) 변경. 트리거 다음 프레임에 1회 판정. 도시 메인 UI 가 포커스인데 표시 전환이 덜 끝났으면 최대 60프레임만 재확인.
- 혜택 문구: 위 트리거, 허가 처리, 공헌도 변경, 화면 크기 변경(GRoot.onSizeChanged) 때만 재조립(최소 0.1초 간격 유지).
- 대기 항목이 있을 때만 적용 재시도(이전과 같은 매 프레임 재시도).
- 훅 6개는 첫 도시 프레임에 등록(시작 시 UIManager 패치는 FairyGUI 조기 초기화로 검은 화면 — Item Rebuild 0.1.0 기록). 등록 실패 시 0.5.4 방식(매 프레임 판정)으로 동작.
검증: 빌드 0/0, RulesTests 196, GrantChecks 54(HUD 판정 경로는 대역 테스트 없음). 게임 확인 전.
DLL `Contribution/releases/0.5.5/` SHA256 206D8279D9BBD4F1B0F9F4AEE7F8AE7E058706E1A5934EEA7CAC8C944FC2516C. 설치 이름 `Mods/Additional_Functions/Restitutor_Additional_Contribution_Goods_Recognition.dll`.
